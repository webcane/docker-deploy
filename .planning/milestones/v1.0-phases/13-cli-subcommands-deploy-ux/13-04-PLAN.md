---
phase: 13-cli-subcommands-deploy-ux
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/filetransfer/upload.go
  - internal/filetransfer/upload_test.go
  - cmd/docker-deploy/main.go
autonomous: true
requirements:
  - 13-04
must_haves:
  truths:
    - "SudoExec is an exported function in package filetransfer with the signature from D-10"
    - "SudoCreds struct with pw []byte field and Zero() method exists in the filetransfer package"
    - "sshRun(client, cmd string, pw []byte) is the single private SSH exec primitive; sshExec and sshExecWithSudoPassword are removed"
    - "Upload() accepts *SudoCreds instead of *string for the password parameter"
    - "All rollback and error paths in Upload() that touch remoteBase call SudoExec, not bare sshRun"
    - "main.go call site uses creds := new(filetransfer.SudoCreds) with defer creds.Zero()"
    - "Step order in SudoExec: (1) direct → (2) cached pw → (3) passwordless sudo -n → (4) interactive up to 3 attempts"
    - "A multi-file deploy to a root-owned path results in exactly one interactive sudo password prompt (warnedOnce=true after first prompt; subsequent files use cached creds.pw)"
  artifacts:
    - path: "internal/filetransfer/upload.go"
      provides: "SudoExec exported func; SudoCreds type; sshRun private func; Upload signature with *SudoCreds"
      exports: ["SudoExec", "SudoCreds"]
    - path: "internal/filetransfer/upload_test.go"
      provides: "TestSudoExec* tests covering all four fallback steps; TestSudoExec_SinglePromptMultiFile test for SC-6"
    - path: "cmd/docker-deploy/main.go"
      provides: "Updated Upload() call site with *SudoCreds and defer creds.Zero()"
  key_links:
    - from: "Upload() in upload.go"
      to: "SudoExec() in upload.go"
      via: "SudoExec(client, cmd, creds, &warnedOnce, verbose) replaces sudoRunWithFallback(cmd)"
      pattern: "SudoExec\\(client"
    - from: "main.go runDeploy()"
      to: "filetransfer.SudoCreds"
      via: "creds := new(filetransfer.SudoCreds); defer creds.Zero()"
      pattern: "filetransfer\\.SudoCreds"
---

<objective>
Refactor the sudo fallback machinery: extract `sudoRunWithFallback` closure into exported `SudoExec`, merge `sshExec`+`sshExecWithSudoPassword` into private `sshRun`, and upgrade password storage from `*string` to `*SudoCreds` with safe zeroing.

Purpose: The closure is untestable and rollback paths in Upload() call sshExec directly (bypassing sudo), which silently fails on root-owned directories. Exporting SudoExec fixes both problems atomically.
Output: SudoExec exported function; SudoCreds type; sshRun unified primitive; Upload signature change; main.go call site updated. All existing tests continue to pass.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md

<interfaces>
<!-- Key signatures and implementation details extracted from codebase — DO NOT deviate. -->

Current Upload() signature (upload.go line 76):
```go
func Upload(ctx context.Context, client *gossh.Client, localDir, remoteBase string, excludes []string, sudoPw *string, warnedOnce *bool, verbose bool) (int, error)
```
New Upload() signature after refactor:
```go
func Upload(ctx context.Context, client *gossh.Client, localDir, remoteBase string, excludes []string, creds *SudoCreds, warnedOnce *bool, verbose bool) (int, error)
```

New SudoCreds type (D-12):
```go
type SudoCreds struct{ pw []byte }
func (c *SudoCreds) Zero() { for i := range c.pw { c.pw[i] = 0 }; c.pw = nil }
```

New SudoExec signature (D-10):
```go
func SudoExec(client *gossh.Client, cmd string, creds *SudoCreds, warnedOnce *bool, verbose bool) error
```

Step order in SudoExec (D-11):
1. Direct: sshRun(client, cmd, nil)
2. Cached: if creds.pw != nil { sshRun(client, cmd, creds.pw) }
3. Passwordless: sshRun(client, "sudo -n sh -c "+ShellQuote(cmd), nil)
4. Interactive: promptSudoPassword() loop up to 3 attempts; on success set creds.pw = []byte(pw)

New sshRun signature (D-13):
```go
func sshRun(client *gossh.Client, cmd string, pw []byte) error
// pw==nil → session.Run(cmd)
// pw!=nil → sudo -S -p '' sh -c <cmd> with stdin pipe; write append(pw, '\n') to stdin
```

Current sshExec (lines 392-403) and sshExecWithSudoPassword (lines 408-430) in upload.go:
- sshExec: session.Run(cmd) — becomes sshRun(client, cmd, nil)
- sshExecWithSudoPassword: sudo -S stdin pipe — becomes sshRun(client, cmd, pw) where pw!=nil
Both functions are DELETED after the merge.

Current sudoRunWithFallback closure (lines 222-288):
- All 8 call sites (lines 291, 316, 322, 325, 344, 353, 356, 366) become SudoExec(client, cmd, creds, warnedOnce, verbose)

Current tryDirectCopy (lines 21-23) and tryPasswordlessSudo (lines 26-29):
- Both are DELETED — their logic moves into SudoExec steps 1 and 3

Current sshExecOutput (lines 432-444) — KEEP unchanged (used for remoteExists and .env backup checks)

Current main.go call site (lines 335-337):
```go
sudoPw := new(string)
warnedOnce := new(bool)
fileCount, err := filetransfer.Upload(context.Background(), client, cwd, resolved.Path, resolved.Excludes, sudoPw, warnedOnce, resolved.Verbose)
```
Becomes (per PATTERNS.md):
```go
creds := new(filetransfer.SudoCreds)
defer creds.Zero()
warnedOnce := new(bool)
fileCount, err := filetransfer.Upload(context.Background(), client, cwd, resolved.Path, resolved.Excludes, creds, warnedOnce, resolved.Verbose)
```
warnedOnce rollup check at lines 342-344 is UNCHANGED.

CRITICAL (D-15 / feedback_sudo_rollback.md): ALL of these call sites in Upload() must become SudoExec(...) not sshRun:
- mkdir -p remoteBase (currently sudoRunWithFallback at line 291)
- mv remoteBase to oldDir (line 316)
- mv staging to remoteBase (line 322)
- rollback mv oldDir to remoteBase (line 325)
- rm -rf oldDir backup (line 344)
- rm -rf remoteBase placeholder (first-deploy, line 353)
- mv staging to remoteBase (first-deploy, line 356)
- cp envBackupPath to envDest (line 366)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SudoCreds type, sshRun, SudoExec to upload.go; remove old functions; update Upload() signature</name>
  <files>internal/filetransfer/upload.go, internal/filetransfer/upload_test.go</files>
  <read_first>
    - internal/filetransfer/upload.go — full file: understand current function layout, all 8 sudoRunWithFallback call sites (lines 291, 316, 322, 325, 344, 353, 356, 366), current tryDirectCopy/tryPasswordlessSudo/sshExec/sshExecWithSudoPassword implementations, and the promptSudoPassword function
    - internal/filetransfer/upload_test.go — existing tests to understand mock SSH server patterns and ensure no breakage
    - .claude/memory/feedback_sudo_rollback.md — confirm which rollback paths require SudoExec
    - .planning/todos/pending/2026-05-26-sudo-exec-refactor.md — review acceptance criteria
  </read_first>
  <behavior>
    - Test TestSudoExec_DirectSuccess: SudoExec succeeds on step 1 (direct cmd exits 0); no password prompt
    - Test TestSudoExec_CachedCreds: SudoExec uses creds.pw on step 2 when direct fails and creds.pw != nil; no new prompt
    - Test TestSudoExec_AllStepsExhausted: when direct, cached, and passwordless all fail and no interactive input, returns error containing "no valid auth path available"
    - Test TestSudoCreds_Zero: Zero() sets all pw bytes to 0 and pw to nil; calling Zero() on empty SudoCreds (nil pw) does not panic
    - Test TestSudoExec_SinglePromptMultiFile: a multi-file deploy to a root-owned path (mock SSH returns permission error until sudo with correct password) triggers the interactive password prompt exactly once; subsequent SudoExec calls within the same deploy reuse creds.pw (step 2) and do NOT prompt again; assert that the mock SSH command counter for the promptSudoPassword code path fires exactly once across all SudoExec calls in the deploy
  </behavior>
  <action>
    In internal/filetransfer/upload.go:

    1. At the top of the file (after imports, before tryDirectCopy), add:
       - SudoCreds struct with unexported pw []byte field
       - Zero() method: range over c.pw setting each byte to 0; set c.pw = nil

    2. Replace sshExec and sshExecWithSudoPassword with a single private sshRun(client *gossh.Client, cmd string, pw []byte) error:
       - Opens a fresh NewSession() per call (CLAUDE.md rule — sessions not reusable)
       - defer session.Close() //nolint:errcheck
       - If pw == nil: return session.Run(cmd) (wrapping error with fmt.Errorf)
       - If pw != nil: StdinPipe() + session.Start("sudo -S -p '' sh -c "+ShellQuote(cmd)) + stdin.Write(append(pw, '\n')) + stdin.Close() + session.Wait()
       Note: write pw as []byte with append(pw, '\n') — do NOT use fmt.Fprintln(stdin, string(pw)) to avoid string conversion

    3. Delete tryDirectCopy, tryPasswordlessSudo — their logic becomes steps 1 and 3 in SudoExec

    4. Add exported SudoExec(client *gossh.Client, cmd string, creds *SudoCreds, warnedOnce *bool, verbose bool) error:
       - Step 1 (direct): if verbose print "[ssh] cmd"; sshRun(client, cmd, nil); if nil return nil
       - Step 2 (cached): if creds.pw != nil: sshRun(client, cmd, creds.pw); if nil return nil
       - Step 3 (passwordless sudo): construct sudoCmd := "sudo -n sh -c "+ShellQuote(cmd); sshRun(client, sudoCmd, nil); if nil return nil; log verbose
       - Step 4 (interactive, up to 3 attempts): if !*warnedOnce: set *warnedOnce=true + print warning if verbose; loop 3x: promptSudoPassword() + sshRun(client, cmd, []byte(pw)); on success: creds.pw = []byte(pw); return nil; if attempt<3: print "Sorry, try again."
       - After loop: return fmt.Errorf("could not write to target directory — no valid auth path available (tried direct copy, passwordless sudo, interactive password)")

    5. In Upload() function:
       - Change signature: replace sudoPw *string with creds *SudoCreds
       - Delete the sudoRunWithFallback closure (lines 222-288)
       - Replace all 8 call sites: sudoRunWithFallback(cmd) → SudoExec(client, cmd, creds, warnedOnce, verbose)
         These are: mkdir-p remoteBase, mv remoteBase→oldDir, mv staging→remoteBase, rollback mv oldDir→remoteBase, rm-rf oldDir, rm-rf placeholder, mv staging→remoteBase (first deploy), cp envBackupPath→envDest
       - For sshExec calls NOT touching remoteBase (staging dir rm on upload error, line 175): keep as sshRun(client, cmd, nil) — direct-only is correct for /tmp cleanup
       - For sshExecOutput calls: replace with equivalent call to the private sshExecOutput (keep sshExecOutput as-is, it uses session.Output and is not replaced by sshRun)
       - The password assignment *sudoPw = pw inside the old closure becomes creds.pw = []byte(pw) inside SudoExec step 4

    In internal/filetransfer/upload_test.go:
    Add TestSudoExec_DirectSuccess, TestSudoExec_CachedCreds, TestSudoExec_AllStepsExhausted, TestSudoCreds_Zero tests using the existing mockSSHServer pattern.

    Add TestSudoExec_SinglePromptMultiFile: configure mockSSHServer to fail direct and passwordless sudo (steps 1, 3) but succeed when given a specific password via sudo -S. Simulate a deploy with multiple files requiring sudo-protected operations: call SudoExec for each of the 8 Upload call sites against this mock. Use a counter variable in the mock handler to track how many times the interactive password path is exercised (detected by receipt of the password on stdin). Assert counter == 1 — the password prompt fires exactly once; all subsequent calls use the cached creds.pw (step 2). Use a bytes.Buffer or io.Pipe to supply "testpassword\n" to the promptSudoPassword stdin reader exactly once.
  </action>
  <verify>
    <automated>go test ./internal/filetransfer/... -run TestSudoExec -v && go test ./internal/filetransfer/... -run TestSudoCreds -v</automated>
  </verify>
  <done>TestSudoExec* and TestSudoCreds* tests pass including TestSudoExec_SinglePromptMultiFile; grep -c "sudoRunWithFallback" internal/filetransfer/upload.go returns 0; grep -c "func sshExec" internal/filetransfer/upload.go returns 0; grep -c "func SudoExec" internal/filetransfer/upload.go returns 1; go build ./internal/filetransfer/... exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Update main.go Upload() call site to use *SudoCreds; ensure full suite passes</name>
  <files>cmd/docker-deploy/main.go</files>
  <read_first>
    - cmd/docker-deploy/main.go — lines 330-350: the Upload() call site with sudoPw *string, warnedOnce, and the rollup check at 342-344
  </read_first>
  <action>
    In cmd/docker-deploy/main.go runDeploy():
    1. Replace `sudoPw := new(string)` (line 335) with `creds := new(filetransfer.SudoCreds)`
    2. Add `defer creds.Zero()` immediately after the creds declaration (before the warnedOnce line)
    3. Update the filetransfer.Upload() call to pass creds in place of sudoPw: the argument at position 6 changes from sudoPw to creds
    4. The warnedOnce variable declaration and the rollup check at lines 342-344 are UNCHANGED — warnedOnce *bool remains in Upload() signature and is threaded through SudoExec

    After this change, go build ./... must compile cleanly because:
    - filetransfer.Upload now expects *filetransfer.SudoCreds (from Task 1)
    - main.go passes creds which is *filetransfer.SudoCreds
  </action>
  <verify>
    <automated>go build ./... && go test ./...</automated>
  </verify>
  <done>go build ./... exits 0; go test ./... exits 0; grep -c "SudoCreds" cmd/docker-deploy/main.go returns at least 2 (new() call + defer Zero() call); grep -c "sudoPw" cmd/docker-deploy/main.go returns 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sudo password → SSH stdin pipe | Password transmitted as []byte via StdinPipe, not embedded in command string |
| SudoCreds.Zero() → memory | Password bytes zeroed after Upload() returns; mitigates in-memory credential exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | sudo password in process listing | mitigate | sudo -S -p '' sh -c <cmd> pattern passes password via stdin pipe, not command line; password never appears in /proc/*/cmdline or SSH server logs [VERIFIED: existing sshExecWithSudoPassword pattern at upload.go:420] |
| T-13-04-02 | Information Disclosure | sudo password in memory | mitigate | SudoCreds stores password as []byte; caller defers creds.Zero() which zeroes all bytes and nils the slice after Upload() returns; standard golang.org/x/crypto convention |
| T-13-04-03 | Tampering | rollback paths use SudoExec | mitigate | D-15: all rollback paths touching remoteBase call SudoExec, not bare sshRun; prevents silent failure on root-owned directories per feedback_sudo_rollback.md |
</threat_model>

<verification>
- go build ./... exits 0
- go test ./... exits 0 (full suite)
- grep -c "sudoRunWithFallback" internal/filetransfer/upload.go returns 0
- grep -c "tryDirectCopy\|tryPasswordlessSudo\|func sshExec\|func sshExecWithSudoPassword" internal/filetransfer/upload.go returns 0
- grep -c "func SudoExec" internal/filetransfer/upload.go returns 1
- grep -c "func sshRun" internal/filetransfer/upload.go returns 1
- grep -c "SudoCreds" cmd/docker-deploy/main.go returns at least 2
- grep -c "sudoPw" cmd/docker-deploy/main.go returns 0
</verification>

<success_criteria>
SudoExec is exported from the filetransfer package with the D-10 signature and D-11 step order. SudoCreds type supports Zero() for safe memory cleanup. sshRun is the unified private primitive. Upload() accepts *SudoCreds. All rollback paths call SudoExec. main.go call site updated. A multi-file deploy to a root-owned path triggers exactly one interactive password prompt (TestSudoExec_SinglePromptMultiFile). Full test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-cli-subcommands-deploy-ux/13-04-SUMMARY.md`
</output>

---
phase: 07-v2-skip-env-override
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - cmd/docker-deploy/main.go
  - internal/filetransfer/upload.go
  - internal/compose/run.go
autonomous: false
requirements:
  - FILES-02
must_haves:
  truths:
    - "docker deploy --skip-env excludes .env from upload and prints a warning"
    - "skip_env: true in deploy.yaml has the same effect as --skip-env"
    - "--verbose prints per-file transfer lines and SSH command lines to stderr"
    - "Without --verbose, all non-blocking warnings are suppressed and a single rollup is printed at end"
    - "With --verbose, warnedOnce is never flipped so every sudo warning prints"
    - "Pre-flight CheckResult checklist is rendered to stderr when --verbose is set"
    - "--verbose prints each SSH command executed in Upload() (mkdir, mv, rm) and its exit code to stderr"
    - "--verbose prints the docker compose up SSH command and its exit code (exit 0 or exit N) to stderr"
    - "go build ./... passes; go test ./... passes"
  artifacts:
    - path: "cmd/docker-deploy/main.go"
      provides: "--skip-env and --verbose flags; FlagOpts population; warning rollup; verbose preflight rendering"
      contains: "skipEnv"
    - path: "internal/filetransfer/upload.go"
      provides: "per-file verbose lines routed to stderr; warnedOnce conditioned on verbose param; SSH exec commands + exit codes logged when verbose"
      contains: "verbose"
    - path: "internal/compose/run.go"
      provides: "RunCompose() with verbose bool param; compose SSH command + exit code logged to stderr when verbose=true"
      contains: "verbose"
  key_links:
    - from: "cmd/docker-deploy/main.go"
      to: "config.FlagOpts{}"
      via: "FlagOpts populated from cobra flag vars and passed to config.Resolve()"
      pattern: "FlagOpts{"
    - from: "internal/filetransfer/upload.go"
      to: "cfg.Verbose"
      via: "verbose bool param added to Upload(); warnedOnce never flipped when verbose=true; sshExec calls log command+exit code when verbose=true"
      pattern: "verbose"
    - from: "cmd/docker-deploy/main.go"
      to: "preflight.RunPreflightChecks()"
      via: "returned CheckResult slice rendered to stderr when cfg.Verbose; discarded otherwise"
      pattern: "CheckResult"
    - from: "cmd/docker-deploy/main.go"
      to: "compose.RunCompose()"
      via: "cfg.Verbose passed as verbose param; RunCompose logs SSH command + exit code to stderr when verbose=true"
      pattern: "RunCompose"
---

<objective>
Wire the Phase 7 features into the command layer, file-transfer layer, and compose layer. This plan:

1. Registers --skip-env and --verbose flags on the deploy cobra command.
2. Replaces all positional Resolve() calls with config.FlagOpts{} struct literals.
3. Updates Upload() in upload.go to accept a verbose bool, route per-file lines to stderr
   (not stdout) when verbose=true, never flip warnedOnce when verbose=true, and log each
   SSH command (mkdir, mv, rm) plus its exit code to stderr when verbose=true.
4. Updates RunCompose() in run.go to accept a verbose bool and log the docker compose SSH
   command string plus its exit code (`exit 0` or `exit N`) to stderr when verbose=true.
5. Implements the warning rollup system in runDeploy(): collects all non-blocking warning
   messages in a []string slice; without --verbose prints only the rollup at end; with
   --verbose prints each warning inline as it occurs.
6. Renders the CheckResult slice from RunPreflightChecks() to stderr when cfg.Verbose.
7. Adds a human-verification checkpoint to confirm the three features work end-to-end.

Purpose: Delivers the user-facing Phase 7 capabilities (skip-env, verbose, expanded
defaults) by wiring the config changes from Plan 01 into the command entry points and
all SSH-executing packages.

Output: Modified cmd/docker-deploy/main.go, internal/filetransfer/upload.go, and
internal/compose/run.go.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/07-v2-skip-env-override/07-CONTEXT.md
@/Users/mniedre/git/docker-deploy/.planning/phases/07-v2-skip-env-override/07-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 — types and signatures executor will use. -->

// FlagOpts (defined in internal/config/config.go after Plan 01):
type FlagOpts struct {
    Host           string
    Path           string
    Excludes       []string
    Force          bool
    ComposeFile    string
    HealthTimeout  int
    HealthInterval int
    SkipEnv        bool
    Verbose        bool
}

// New Resolve() signature (after Plan 01):
func Resolve(opts config.FlagOpts, file config.FileConfig, projectName string, localDir string) (config.Config, error)

// Config new fields (after Plan 01):
type Config struct {
    // ... existing fields ...
    SkipEnv  bool
    Verbose  bool
}

// Current Upload() signature (to be updated in Task 1 of this plan):
func Upload(ctx context.Context, client *gossh.Client, localDir, remoteBase string, excludes []string, sudoPw *string, warnedOnce *bool) (int, error)

// Target Upload() signature:
func Upload(ctx context.Context, client *gossh.Client, localDir, remoteBase string, excludes []string, sudoPw *string, warnedOnce *bool, verbose bool) (int, error)

// Current RunCompose() signature (internal/compose/run.go):
func RunCompose(ctx context.Context, client *gossh.Client, remotePath, composeFile string) error

// Target RunCompose() signature:
func RunCompose(ctx context.Context, client *gossh.Client, remotePath, composeFile string, verbose bool) error

// RunPreflightChecks() (unchanged from Phase 5 — returns []CheckResult):
func RunPreflightChecks(ctx context.Context, client SSHRunner, cfg config.Config) ([]CheckResult, error)

// CheckResult (unchanged from Phase 5):
type CheckResult struct {
    Name    string // e.g. "docker-installed"
    Status  string // "pass", "warn", or "fail"
    Message string
}
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upload() verbose param — per-file stderr lines, warnedOnce conditioned on verbose, SSH exec command logging</name>
  <files>internal/filetransfer/upload.go</files>
  <read_first>
    - internal/filetransfer/upload.go (full file — current Upload() signature at line 67, per-file fmt.Fprintf(os.Stdout) at line 149, sshExec calls at lines 156/205/230-250/259-264, sudoRunWithFallback closure at lines 169-202 with warnedOnce usage)
    - .planning/phases/07-v2-skip-env-override/07-CONTEXT.md (D-01, D-02, D-03, D-04 — output routing, warnedOnce behavior, verbose per-file line format, SSH command logging)
    - .planning/phases/07-v2-skip-env-override/07-01-SUMMARY.md (confirm Plan 01 shipped)
  </read_first>
  <behavior>
    - Upload(ctx, client, localDir, remoteBase, excludes, sudoPw, warnedOnce, verbose bool) — signature gains verbose bool as last param
    - When verbose=true: per-file line `  -> relative/path` written to os.Stderr (not os.Stdout)
    - When verbose=false: per-file lines are suppressed entirely (no output per file)
    - The "Uploading N files..." progress line stays on os.Stdout in both modes
    - warnedOnce logic in sudoRunWithFallback: when verbose=true, the `!*warnedOnce` guard is bypassed — every password warning prints; `*warnedOnce` is never set to true
    - When verbose=false: existing warnedOnce behavior is unchanged (first warning prints, subsequent ones suppressed)
    - SSH exec commands (every sshExec / sshExecOutput call within Upload()) log `[ssh] <cmd>` to stderr before running and `→ exit 0` or `→ exit N` to stderr after, when verbose=true. This covers the staging cleanup rm, the mkdir-p, the atomic mv/rm commands, and the remoteExists test -d call.
  </behavior>
  <action>
1. Add verbose bool as the last parameter to Upload():

    func Upload(ctx context.Context, client *gossh.Client, localDir, remoteBase string, excludes []string, sudoPw *string, warnedOnce *bool, verbose bool) (int, error)

2. In the file-upload loop (currently at line ~149), change the per-file output:
   - Before: fmt.Fprintf(os.Stdout, "  -> %s\n", relPath)
   - After: if verbose { fmt.Fprintf(os.Stderr, "  -> %s\n", relPath) }
   The "Uploading N files..." line at line ~78 remains on os.Stdout unchanged.

3. In the sudoRunWithFallback closure, change the warnedOnce guard:
   - Before:
       if !*warnedOnce {
           fmt.Fprintf(os.Stderr, "WARNING: passwordless sudo not configured; you may be prompted for a password\n")
           *warnedOnce = true
       }
   - After:
       if verbose || !*warnedOnce {
           fmt.Fprintf(os.Stderr, "WARNING: passwordless sudo not configured; you may be prompted for a password\n")
           if !verbose {
               *warnedOnce = true
           }
       }
   This allows every instance of the warning to print when verbose=true, while preserving the once-only behavior when verbose=false.

4. Add a package-private helper sshExecVerbose that wraps sshExec and logs when verbose=true:

    func sshExecVerbose(client *gossh.Client, cmd string, verbose bool) error {
        if verbose {
            fmt.Fprintf(os.Stderr, "[ssh] %s\n", cmd)
        }
        err := sshExec(client, cmd)
        if verbose {
            if err == nil {
                fmt.Fprintf(os.Stderr, "  → exit 0\n")
            } else {
                // Extract exit code if available; fall back to "exit ?" if not
                var exitErr *gossh.ExitError
                if errors.As(err, &exitErr) {
                    fmt.Fprintf(os.Stderr, "  → exit %d\n", exitErr.ExitStatus())
                } else {
                    fmt.Fprintf(os.Stderr, "  → exit ?\n")
                }
            }
        }
        return err
    }

   Import "errors" and gossh "golang.org/x/crypto/ssh" are already present in upload.go;
   gossh.ExitError is defined in that package. Add "errors" to imports if not already present
   (check: upload.go does not currently import "errors" — add it).

5. Replace all direct sshExec() calls within Upload() (NOT in sshExec itself, NOT in
   tryDirectCopy/tryPasswordlessSudo — those remain as-is since they are always silent) with
   sshExecVerbose(). Specifically, replace:
   - The staging cleanup: `_ = sshExec(client, fmt.Sprintf("rm -rf %s", ...))` inside uploadErr
     block (line ~156) — this is a silent best-effort cleanup; leave this one as plain sshExec
     since it is already a silent discard
   - Inside sudoRunWithFallback the calls to tryDirectCopy and tryPasswordlessSudo remain
     untouched (they call sshExec internally and are always silent)
   - The sudoRunWithFallback cmd invocations (mkdir -p, mv, rm) — these are the calls that
     matter for verbose output. Replace the `sshExec(client, sudoCmd)` inside the interactive
     sudo attempt loop with sshExecVerbose; and thread verbose into sudoRunWithFallback so it
     can call sshExecVerbose for the tryDirectCopy/tryPasswordlessSudo paths too.

   Simpler approach: instead of threading verbose into helper functions, replace the
   sudoRunWithFallback closure body so that each command attempt logs when verbose=true.
   The closure already captures verbose from the outer Upload() scope. Pattern:

   In tryDirectCopy path inside sudoRunWithFallback:
       if verbose { fmt.Fprintf(os.Stderr, "[ssh] %s\n", cmd) }
       ok := tryDirectCopy(client, cmd)
       if verbose && ok  { fmt.Fprintf(os.Stderr, "  → exit 0\n") }
       if ok { return nil }

   In tryPasswordlessSudo path:
       sudoCmd := fmt.Sprintf("sudo -n sh -c %s", ShellQuote(cmd))
       if verbose { fmt.Fprintf(os.Stderr, "[ssh] %s\n", sudoCmd) }
       ok := tryPasswordlessSudo(client, cmd)
       if verbose && ok  { fmt.Fprintf(os.Stderr, "  → exit 0\n") }
       if ok { return nil }

   In the interactive sudo attempt:
       if verbose { fmt.Fprintf(os.Stderr, "[ssh] %s\n", "(sudo password cmd redacted)") }
       // run attempt; on success:
       if verbose { fmt.Fprintf(os.Stderr, "  → exit 0\n") }

   Note: redact the interactive sudo command in verbose output (it contains the literal
   password); print "[ssh] (sudo password cmd redacted)" instead of the full command string.

   Also log the remoteExists test-d call. Since remoteExists uses sshExecOutput (a separate
   helper), add an analogous sshExecOutputVerbose or inline verbose logging around the
   remoteExists call in Upload():
       if verbose { fmt.Fprintf(os.Stderr, "[ssh] test -d %s\n", ShellQuote(remoteBase)) }
       existsBefore, err := remoteExists(client, remoteBase)
       if verbose && err == nil { fmt.Fprintf(os.Stderr, "  → exit 0\n") }

6. Update the Upload() doc comment to describe the verbose parameter, stderr routing change,
   and SSH command logging behavior.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/docker-deploy && go build ./internal/filetransfer/... && go test ./internal/filetransfer/...</automated>
  </verify>
  <done>upload.go compiles; Upload() has verbose bool param; per-file lines go to stderr only when verbose=true; "Uploading N files..." stays on stdout; warnedOnce guard conditioned on verbose; SSH commands (mkdir, mv, rm) and their exit codes logged to stderr when verbose=true</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: RunCompose() verbose param — compose SSH command + exit code logged to stderr</name>
  <files>internal/compose/run.go</files>
  <read_first>
    - internal/compose/run.go (full file — RunCompose() signature at line 46, cmd construction at line 58, handleWait() at line 182, session.Start at lines 136 and 157, exit code print in handleWait at line 189)
    - .planning/phases/07-v2-skip-env-override/07-CONTEXT.md (D-01 category (2): each SSH command and its exit code to stderr)
  </read_first>
  <behavior>
    - RunCompose(ctx, client, remotePath, composeFile, verbose bool) — signature gains verbose bool as last param
    - When verbose=true: print `[ssh] <cmd>` to os.Stderr immediately before session.Start(cmd) (both in the TTY path and the non-TTY path)
    - When verbose=true and exit code is 0: print `  → exit 0` to os.Stderr after Wait() returns nil
    - When verbose=true and exit code is N != 0: handleWait already prints "Deploy failed: docker compose exited with code N" to stderr; additionally print `  → exit N` in the same format as other SSH commands for consistency
    - When verbose=false: no change to existing behavior
    - The compose command string (cmd variable at line 58) is already constructed before Start() — log it directly
  </behavior>
  <action>
1. Add verbose bool as the last parameter to RunCompose():

    func RunCompose(ctx context.Context, client *gossh.Client, remotePath, composeFile string, verbose bool) error

2. After `cmd` is constructed (line 58) and before session.Start(), add verbose logging.
   Both the TTY path (line ~157) and the non-TTY path (line ~136) call session.Start(cmd).
   Add the print immediately before each session.Start(cmd) call:

    if verbose {
        fmt.Fprintf(os.Stderr, "[ssh] %s\n", cmd)
    }

   There are two session.Start(cmd) calls (one in the non-TTY block, one in the PTY block).
   Add the verbose print before BOTH.

3. For exit code logging after successful completion (exit 0):
   - In the non-TTY path, handleWait(session.Wait()) returns nil on success. Capture
     the return value and add:
       waitErr := handleWait(session.Wait())
       if verbose && waitErr == nil {
           fmt.Fprintf(os.Stderr, "  → exit 0\n")
       }
       return waitErr
   - In the PTY path (last line of function), similarly:
       waitErr := handleWait(session.Wait())
       if verbose && waitErr == nil {
           fmt.Fprintf(os.Stderr, "  → exit 0\n")
       }
       return waitErr

4. For non-zero exit codes: handleWait() already prints
   "Deploy failed: docker compose exited with code N" to stderr. Add `  → exit N` logging
   by extending handleWait() to accept verbose bool OR by checking the error in RunCompose
   after handleWait returns. The simpler approach: after capturing waitErr, check if it
   contains an exit code and print the consistent format:

       if verbose && waitErr != nil {
           var exitErr *gossh.ExitError
           if errors.As(waitErr, &exitErr) {
               fmt.Fprintf(os.Stderr, "  → exit %d\n", exitErr.ExitStatus())
           }
       }

   Add this block in both the non-TTY and PTY Wait() return paths.

5. Add "errors" to imports if not already present (run.go currently imports "errors" at
   line 8 — confirm before adding).

6. Update the RunCompose() doc comment to describe the verbose parameter and stderr logging.

   Note: RunCompose() call site wiring in main.go (passing cfg.Verbose) is handled by Task 2.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/docker-deploy && go build ./... && go test ./internal/compose/...</automated>
  </verify>
  <done>run.go compiles; RunCompose() has verbose bool param; compose SSH command logged to stderr before Start(); exit 0 logged after successful Wait(); exit N logged (in addition to existing error message) after failed Wait(); all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: main.go — flags, FlagOpts call sites, warning rollup, verbose preflight rendering, RunCompose wiring</name>
  <files>cmd/docker-deploy/main.go</files>
  <read_first>
    - cmd/docker-deploy/main.go (full file — cobra flag vars at lines 37-41, RunE at 44-49, runDeploy() and runDryRun() signatures and Resolve() call sites at lines 88-91 and 155-158, Upload() call at lines 252, RunCompose() call, preflight.RunPreflightChecks call at line 200)
    - .planning/phases/07-v2-skip-env-override/07-CONTEXT.md (D-02, D-03, D-06, D-07, D-09 — rollup message copy, warning routing, flag names, skip-env warning copy)
    - internal/config/config.go (FlagOpts struct fields from Plan 01)
  </read_first>
  <behavior>
    - Two new flag vars in main(): skipEnv bool and verbose bool
    - --skip-env and --verbose registered as cobra persistent flags on the deploy cmd (same level as --host, --force)
    - runDeploy() and runDryRun() signatures gain skipEnv bool and verbose bool params
    - Both Resolve() calls replaced with config.Resolve(config.FlagOpts{...}, fileConfig, projectName, cwd)
    - Upload() call updated to pass cfg.Verbose as the new verbose param
    - RunCompose() call updated to pass cfg.Verbose as the new verbose param
    - preflight.RunPreflightChecks() return value captured as results []preflight.CheckResult; when cfg.Verbose, render results to stderr: each result as `  [STATUS] name: message`
    - Warning rollup in runDeploy(): collect non-blocking warning strings in a var warnings []string; append to it whenever a non-blocking condition is met (skip-env active, any preflight warn); at end of runDeploy(), if len(warnings) > 0 and !cfg.Verbose, print: `WARN: there are some warnings during deployment. For more details use --verbose flag\n` to stderr
    - When cfg.Verbose=true, each warning is printed inline (to stderr) as it occurs; no rollup message
    - runDryRun() also updated: Resolve() call uses FlagOpts; no rollup system needed for dry-run (dry-run exits after connectivity check)
  </behavior>
  <action>
1. Add two flag vars alongside the existing flag vars in main():
    var skipEnv bool
    var verbose bool

2. Register flags on cmd (after the existing cmd.Flags() calls):
    cmd.Flags().BoolVar(&skipEnv, "skip-env", false, "Exclude .env from upload, leaving remote .env unchanged")
    cmd.Flags().BoolVar(&verbose, "verbose", false, "Print per-file transfer lines, SSH commands, and pre-flight checklist to stderr")

3. Update RunE to pass skipEnv and verbose to both run functions:
    if dryRun {
        return runDryRun(host, path, excludes, force, composeFile, skipEnv, verbose)
    }
    return runDeploy(host, path, excludes, force, composeFile, skipEnv, verbose)

4. Update runDryRun() signature to add skipEnv bool and verbose bool params. Replace the positional Resolve() call (currently line 90-91) with:
    resolved, err := config.Resolve(config.FlagOpts{
        Host:        host,
        Path:        path,
        Excludes:    excludes,
        Force:       force,
        ComposeFile: "docker-compose.yml", // sentinel: skips auto-detect; value is unused in dry-run
        SkipEnv:     skipEnv,
        Verbose:     verbose,
    }, fileConfig, projectName, cwd)

5. Update runDeploy() signature to add skipEnv bool and verbose bool params. Replace the positional Resolve() call (currently line 157-158) with:
    resolved, err := config.Resolve(config.FlagOpts{
        Host:           host,
        Path:           path,
        Excludes:       excludes,
        Force:          force,
        ComposeFile:    composeFile,
        SkipEnv:        skipEnv,
        Verbose:        verbose,
    }, fileConfig, projectName, cwd)

6. After Resolve() in runDeploy(), initialize the warning collector:
    var warnings []string

7. After cfg.SkipEnv is set, add the skip-env warning:
    if resolved.SkipEnv {
        msg := "WARNING: .env not uploaded — remote .env left unchanged"
        if resolved.Verbose {
            fmt.Fprintln(os.Stderr, msg)
        } else {
            warnings = append(warnings, msg)
        }
    }

8. Update the RunPreflightChecks call to capture results:
    results, err := preflight.RunPreflightChecks(context.Background(), preflight.NewSSHRunner(client), resolved)
    if err != nil {
        fmt.Fprintf(os.Stderr, "Pre-flight failed: %v\n", err)
        return err
    }

   After the error check, render the checklist when verbose:
    if resolved.Verbose {
        for _, r := range results {
            fmt.Fprintf(os.Stderr, "  [%s] %s: %s\n", strings.ToUpper(r.Status), r.Name, r.Message)
        }
    } else {
        // Collect warn-status results for rollup
        for _, r := range results {
            if r.Status == "warn" {
                warnings = append(warnings, fmt.Sprintf("pre-flight: %s: %s", r.Name, r.Message))
            }
        }
    }

   Note: the existing inline warn prints inside checkDaemon(), checkRootUser(), checkTargetDir(), checkDockerGroup() still fire when not verbose. Phase 7 does not modify preflight/checks.go. The rollup accumulation above is additive context for the end-of-deploy message only.

9. Update the Upload() call to pass cfg.Verbose:
    fileCount, err := filetransfer.Upload(context.Background(), client, cwd, resolved.Path, resolved.Excludes, sudoPw, warnedOnce, resolved.Verbose)

10. Update the RunCompose() call to pass cfg.Verbose:
    err = compose.RunCompose(ctx, client, resolved.Path, resolved.ComposeFile, resolved.Verbose)

11. Add the rollup print before the "Deploy complete" success line at the bottom of runDeploy():
     if len(warnings) > 0 && !resolved.Verbose {
         fmt.Fprintln(os.Stderr, "WARN: there are some warnings during deployment. For more details use --verbose flag")
     }

12. The "strings" import is already present; add "strings" to the imports only if not already present (check: strings.EqualFold is used in existing code, so it is already imported).
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/docker-deploy && go build ./... 2>&1</automated>
  </verify>
  <done>go build ./... exits 0; --skip-env and --verbose flags are registered; Resolve() call sites use FlagOpts{}; Upload() receives cfg.Verbose; RunCompose() receives cfg.Verbose; CheckResult slice is rendered to stderr when --verbose; rollup message present when warnings exist and --verbose is not set</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three Phase 7 features are now implemented:
    1. Expanded defaultExcludes (16 entries — .github/, .claude/, .planning/, etc.)
    2. --skip-env flag (and skip_env: true in deploy.yaml) — excludes .env from upload, prints warning
    3. --verbose flag — per-file transfer lines, SSH command lines (with exit codes), pre-flight checklist, all warnings inline; without it, single rollup at end

    Build has been verified clean with go build ./...
  </what-built>
  <how-to-verify>
    Run each of the following in the project root:

    A. Verify flags are registered:
       docker deploy --help
       Expected: --skip-env and --verbose appear in the flag list alongside --host, --force, etc.

    B. Verify expanded defaults (dry-run + grep):
       docker deploy --dry-run --host ssh://user@yourhost:22
       (or review output of): go run ./cmd/docker-deploy/... deploy --help
       Then: grep -c "\.github/" internal/config/config.go
       Expected: 1

    C. Verify --skip-env warning (no real host needed — just verify the flag resolves):
       Build and run: go build -o /tmp/test-deploy ./cmd/docker-deploy && echo "testing flag registration"
       (Full end-to-end skip-env test requires a real SSH host. Verify the flag registers without error.)

    D. Verify --verbose SSH command logging (if a real SSH host is available):
       docker deploy --host ssh://user@yourhost:22 --verbose 2>/tmp/verbose.txt
       cat /tmp/verbose.txt
       Expected output contains ALL of the following:
         - Lines starting with "  ->" (per-file transfer)
         - Lines starting with "[ssh] " (SSH commands: mkdir, mv, rm, docker compose up -d)
         - Lines starting with "  → exit " (exit codes for each SSH command)
         - "[PASS]" / "[WARN]" lines from pre-flight checklist
       docker deploy --host ssh://user@yourhost:22 2>/tmp/normal.txt
       cat /tmp/normal.txt
       Expected: does NOT contain "  ->" or "[ssh] " lines; if any warnings, contains the rollup line.

    E. Verify compose SSH command appears in verbose output:
       grep "\[ssh\] docker compose" /tmp/verbose.txt
       Expected: one line containing "docker compose -f ... up -d --remove-orphans"
       grep "→ exit 0" /tmp/verbose.txt
       Expected: at least one "→ exit 0" line (compose success)

    F. Verify go test passes:
       go test ./...
       Expected: all tests pass (PASS, no FAIL lines)
  </how-to-verify>
  <resume-signal>Type "approved" if flags appear in --help, SSH command lines (with exit codes) appear in --verbose output, and go test ./... passes. Describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → --skip-env flag | Operator intentionally suppresses .env upload; remote secrets silently diverge from local |
| operator → --verbose flag | Detailed output (file names, SSH commands) written to stderr; if stderr is logged, file paths are exposed |
| warning rollup → operator | Non-verbose mode hides individual warnings; operator may miss actionable issues |
| --verbose → SSH command log | SSH command strings (including remote paths) are printed to stderr; interactive sudo command is redacted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | --skip-env | mitigate | Per D-09: always print `WARNING: .env not uploaded — remote .env left unchanged` to stderr; without --verbose this becomes the rollup message so it is never silently swallowed — the rollup fires whenever warnings > 0 |
| T-07-02-02 | Repudiation | warning rollup | mitigate | The rollup message `WARN: there are some warnings during deployment. For more details use --verbose flag` is always printed to stderr when any non-blocking warning occurred; operator cannot claim ignorance of warnings |
| T-07-02-03 | Information Disclosure | --verbose stderr output | accept | Per-file paths and SSH commands are written to stderr which is operator-controlled; logging pipelines that capture stderr receive file names — acceptable for a deploy tool used by the operator themselves |
| T-07-02-04 | Elevation of Privilege | Upload() verbose param | accept | verbose param is a read-only behavior switch; it does not change SSH commands, paths, or auth logic; no privilege escalation risk |
| T-07-02-05 | Information Disclosure | --verbose sudo command log | mitigate | Interactive sudo commands contain the literal sudo password; upload.go Task 1 action step 5 specifies printing "[ssh] (sudo password cmd redacted)" instead of the full command string when verbose=true |
</threat_model>

<verification>
## Phase 7 Plan 02 Checks

1. `grep -c "skip-env" cmd/docker-deploy/main.go` returns >= 1 (flag registered)
2. `grep -c "verbose" cmd/docker-deploy/main.go` returns >= 1 (flag registered)
3. `grep -c "FlagOpts{" cmd/docker-deploy/main.go` returns >= 2 (both Resolve() call sites updated)
4. `grep -c "func Upload" internal/filetransfer/upload.go` returns 1 and signature contains "verbose bool"
5. `grep -c "warnings" cmd/docker-deploy/main.go` returns >= 3 (slice declared, appended to, printed)
6. `grep -c "WARN: there are some warnings" cmd/docker-deploy/main.go` returns 1 (rollup message)
7. `grep -c "WARNING: .env not uploaded" cmd/docker-deploy/main.go` returns 1 (skip-env warning copy)
8. `grep -c "func RunCompose" internal/compose/run.go` returns 1 and signature contains "verbose bool"
9. `grep -v '^//' internal/compose/run.go | grep -c "\[ssh\]"` returns >= 1 (compose SSH command logging)
10. `grep -v '^//' internal/filetransfer/upload.go | grep -c "\[ssh\]"` returns >= 1 (upload SSH command logging)
11. `go build ./...` exits 0
12. `go test ./...` exits 0
13. `docker deploy --help` shows --skip-env and --verbose in flag list
</verification>

<success_criteria>
- go build ./... passes with no errors
- go test ./... passes with no failures
- --skip-env flag registered on deploy command; triggers .env exclusion via resolved.SkipEnv; prints `WARNING: .env not uploaded — remote .env left unchanged` to stderr (inline with --verbose, as part of rollup without)
- --verbose flag registered on deploy command; when set: per-file lines written to stderr, SSH commands (mkdir, mv, rm) and their exit codes written to stderr, docker compose SSH command and exit code written to stderr, preflight CheckResult checklist rendered to stderr, every sudo warning printed; when not set: no per-file lines, no SSH command lines, single rollup `WARN: there are some warnings during deployment. For more details use --verbose flag` printed to stderr if any warnings occurred
- Both runDeploy() and runDryRun() use config.FlagOpts{} struct literal for Resolve() call
- Upload() call passes cfg.Verbose as last argument
- RunCompose() call passes cfg.Verbose as last argument
- With --verbose: `[ssh] docker compose -f ... up -d --remove-orphans` appears in stderr output, followed by `  → exit 0` on success
</success_criteria>

<output>
After completion, create `.planning/phases/07-v2-skip-env-override/07-02-SUMMARY.md`
</output>

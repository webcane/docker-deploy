---
phase: 10-add-phase-autosuggestion
plan: 05
type: execute
wave: 2
depends_on: [03]
files_modified:
  - .goreleaser.yaml
  - INSTALL.md
autonomous: true
gap_closure: true
requirements: []
tags:
  - goreleaser
  - homebrew
  - documentation
  - completion

must_haves:
  truths:
    - "Goreleaser-produced release tarballs include `contrib/_docker-deploy` and `contrib/docker-deploy.bash` alongside the binary"
    - "The Homebrew formula generated by goreleaser installs `_docker-deploy` to `share/zsh/site-functions/` and `docker-deploy.bash` to `share/bash-completion/completions/`"
    - "After `brew install`, zsh completions work with zero user fpath configuration (Homebrew prefix is already in $FPATH on macOS)"
    - "INSTALL.md has a `Shell Completions` section explaining the Homebrew (automatic) and manual (contrib/install-completions.sh) install paths"
    - "README.md is NOT modified — completions are intentionally not advertised there (per D-09)"
    - "The hidden `completion` subcommand is NOT mentioned in INSTALL.md or README.md (per D-02, D-09)"
  artifacts:
    - path: ".goreleaser.yaml"
      provides: "extra_files including contrib/ and brews.install lines for completions"
      contains: "_docker-deploy"
    - path: "INSTALL.md"
      provides: "Shell Completions section"
      contains: "## Shell Completions"
  key_links:
    - from: ".goreleaser.yaml (archives.extra_files / brews.install)"
      to: "contrib/_docker-deploy and contrib/docker-deploy.bash"
      via: "release tarball inclusion + brew install lines"
      pattern: "_docker-deploy"
---

<objective>
Finish the rollout: ship the completion files in releases, install them via Homebrew, and document the install paths for users.

Purpose: Per CONTEXT.md D-06, D-07, D-09. Goreleaser is the release pipeline — it must bundle the `contrib/` files into tarballs and update the Homebrew formula to install them. INSTALL.md gets a documented section so users know completions exist; README.md stays untouched per D-09.

Output:
- `.goreleaser.yaml` updated with `archives[].extra_files` (or equivalent) so `contrib/_docker-deploy` and `contrib/docker-deploy.bash` are packaged into each release tarball
- `.goreleaser.yaml` `brews.install` block extended with two lines that install the completion files into Homebrew's standard zsh and bash completion paths
- `INSTALL.md` gains a "Shell Completions" section describing the Homebrew (automatic) path first and the manual `contrib/install-completions.sh` fallback second
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-add-phase-autosuggestion/10-CONTEXT.md
@.planning/notes/completion-rework-design.md
@.goreleaser.yaml
@INSTALL.md

<interfaces>
<!-- Goreleaser v2 archive/brews schema fields used in this plan -->

```yaml
archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    files:
      - src: contrib/_docker-deploy
      - src: contrib/docker-deploy.bash
```

Note: in goreleaser v2 the `archives[].files:` key (with `src:` entries) is the correct way to add EXTRA files to per-build archive tarballs. The top-level `release.extra_files` adds files to the GitHub release page but NOT to the per-arch tarballs that the Homebrew formula extracts.

Homebrew formula install (Ruby HEREDOC inside .goreleaser.yaml):
```ruby
bin.install "docker-deploy"
(lib/"docker/cli-plugins").install_symlink bin/"docker-deploy"
(share/"zsh/site-functions").install "_docker-deploy"
(share/"bash-completion/completions").install "docker-deploy.bash"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add contrib files to release tarballs and update brews.install in .goreleaser.yaml</name>
  <files>.goreleaser.yaml</files>
  <read_first>
    - .goreleaser.yaml (current content, full file)
    - .planning/phases/10-add-phase-autosuggestion/10-CONTEXT.md (D-06, D-07 — extra_files and brews.install)
    - .planning/notes/completion-rework-design.md (Homebrew install path rationale)
  </read_first>
  <action>
    Edit `.goreleaser.yaml`:

    1) Modify the `archives:` section. The existing entry is:
       ```
       archives:
         - format: tar.gz
           name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
       ```
       Add a `files:` key to that entry listing the two completion files as `src:` entries. The binary is added automatically by goreleaser — do NOT list it. Use this exact structure (preserve YAML indentation, 2 spaces):
       ```
       archives:
         - format: tar.gz
           name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
           files:
             - src: contrib/_docker-deploy
             - src: contrib/docker-deploy.bash
       ```

    2) Modify the `brews[0].install` block. Current value:
       ```
       install: |
         bin.install "docker-deploy"
         (lib/"docker/cli-plugins").install_symlink bin/"docker-deploy"
       ```
       Append two new lines (Ruby syntax inside the YAML `|` literal block) AFTER the existing two:
       ```
       install: |
         bin.install "docker-deploy"
         (lib/"docker/cli-plugins").install_symlink bin/"docker-deploy"
         (share/"zsh/site-functions").install "_docker-deploy"
         (share/"bash-completion/completions").install "docker-deploy.bash"
       ```

       Indentation: each Ruby line is indented with the same leading whitespace as the existing two (2 spaces inside the `|` block, on top of YAML's own 2-space nesting).

    3) Do NOT modify any other section of `.goreleaser.yaml` — leave `builds`, `checksum`, `signs`, `release`, `snapshot`, `brews[0].test`, and `brews[0].custom_block` exactly as-is. Do not touch `repository`, `homepage`, `description`, or `license`.

    4) After editing, run `goreleaser check` if the binary is available locally to validate the YAML. If not available, fall back to `python3 -c 'import yaml,sys; yaml.safe_load(open(".goreleaser.yaml"))'` to confirm parse-validity. Surface any errors instead of "fixing" by guessing.
  </action>
  <verify>
    <automated>grep -q 'contrib/_docker-deploy' .goreleaser.yaml && grep -q 'contrib/docker-deploy\.bash' .goreleaser.yaml && grep -q 'share/"zsh/site-functions"' .goreleaser.yaml && grep -q 'share/"bash-completion/completions"' .goreleaser.yaml && python3 -c 'import yaml; yaml.safe_load(open(".goreleaser.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - `.goreleaser.yaml` parses as valid YAML (`python3 -c 'import yaml; yaml.safe_load(open(".goreleaser.yaml"))'` exits 0)
    - The `archives[0]` entry contains a `files:` list with two `src:` entries: `contrib/_docker-deploy` and `contrib/docker-deploy.bash`
    - The `brews[0].install` block contains BOTH of these literal lines (in addition to the original two):
      - `(share/"zsh/site-functions").install "_docker-deploy"`
      - `(share/"bash-completion/completions").install "docker-deploy.bash"`
    - The original two `bin.install "docker-deploy"` and `(lib/"docker/cli-plugins").install_symlink bin/"docker-deploy"` lines are PRESERVED
    - No other top-level keys (`builds`, `checksum`, `signs`, `release`, `snapshot`) were modified — verifiable by `git diff` showing changes only inside `archives:` and `brews[0].install`
    - If `goreleaser` is installed: `goreleaser check` exits 0
  </acceptance_criteria>
  <done>Goreleaser will bundle the two completion files into every release tarball, and the Homebrew formula will install them into Homebrew's standard fpath and bash-completion directories.</done>
</task>

<task type="auto">
  <name>Task 2: Add Shell Completions section to INSTALL.md</name>
  <files>INSTALL.md</files>
  <read_first>
    - INSTALL.md (current content, lines 1-72)
    - .planning/phases/10-add-phase-autosuggestion/10-CONTEXT.md (D-09 — INSTALL.md only; README.md not touched; hidden subcommand not mentioned)
  </read_first>
  <action>
    Edit `INSTALL.md`. Append a new section titled `## Shell Completions` at the end of the file (after the existing `## go install` section). The section must:

    1) Start with a one-paragraph intro: completions are provided for **bash** and **zsh** only (cite the supported shells).

    2) First subsection `### Homebrew (automatic)`:
       - State that `brew install webcane/docker-deploy/docker-deploy` (or `brew install docker-deploy` if the tap is already added) installs the completion files automatically.
       - For zsh, the file lands in Homebrew's `share/zsh/site-functions/` which is already on `$FPATH` on Homebrew-managed macOS — no further action needed; just restart the shell or run `compinit`.
       - For bash, the file lands in `share/bash-completion/completions/` which is loaded by the `bash-completion` package; mention `brew install bash-completion` (or `bash-completion@2`) as a prerequisite if the user does not already have it.

    3) Second subsection `### Manual install (non-Homebrew)`:
       - Provide the curl one-liner that downloads and runs `contrib/install-completions.sh` from a release tag, for example:
         ```bash
         curl -fsSL https://raw.githubusercontent.com/webcane/docker-deploy/master/contrib/install-completions.sh | sh
         ```
         Or pinned to a tag (recommended):
         ```bash
         curl -fsSL https://raw.githubusercontent.com/webcane/docker-deploy/v0.12.2/contrib/install-completions.sh | sh
         ```
       - State that the script auto-detects the shell (bash or zsh) and installs the matching file to the right location.
       - State the fallback directories it uses (`~/.zsh/completions/` and `~/.bash_completion.d/`) and the one-line `~/.zshrc` / `~/.bashrc` snippet the user must add when those fallbacks are used (fpath append for zsh; `source` for bash).

    4) Third subsection `### Verify`:
       - One-line check the user can run to confirm completion works, e.g. starting `docker deploy --<TAB>` should list flag names like `--host`, `--path`, `--compose-file`.

    Style rules:
    - Match the existing heading style and code-fence style used in `INSTALL.md` (triple-backtick fences with language hints like `bash` and `json`).
    - Do NOT mention the `docker-deploy completion bash|zsh` subcommand anywhere — that is hidden by design (D-02). Users should never invoke it directly; they install pre-generated files.
    - Do NOT touch `README.md` (D-09 explicitly excludes README).
    - Do not edit any pre-existing section of `INSTALL.md`. Only APPEND the new section.

    Total length: target 40-70 lines for the new section.
  </action>
  <verify>
    <automated>grep -q '^## Shell Completions' INSTALL.md && grep -q 'Homebrew' INSTALL.md && grep -q 'install-completions\.sh' INSTALL.md && ! grep -q 'docker-deploy completion ' INSTALL.md && ! grep -q 'docker deploy completion' INSTALL.md && ! git diff README.md 2>/dev/null | grep -q '^[+-]'</automated>
  </verify>
  <acceptance_criteria>
    - `INSTALL.md` contains a top-level heading exactly `## Shell Completions`
    - The section contains the three subsections: `### Homebrew (automatic)`, `### Manual install (non-Homebrew)`, `### Verify`
    - The section references `contrib/install-completions.sh` (at least one occurrence)
    - The section references both `bash` and `zsh` (case-insensitive match for each is fine)
    - The section does NOT contain the literal strings `docker-deploy completion bash`, `docker-deploy completion zsh`, or `docker deploy completion` — the hidden subcommand is not exposed (D-02)
    - The section references `share/zsh/site-functions` and `share/bash-completion/completions` at least once
    - `README.md` is UNCHANGED — `git diff README.md` shows no modifications
    - Pre-existing sections (`## Install script`, `## Homebrew`, `## Manual binary`, `## go install`) are unchanged — `git diff INSTALL.md` shows additions only, no edits to those sections
  </acceptance_criteria>
  <done>INSTALL.md tells users how to enable completions through both Homebrew (automatic) and the manual install script, without exposing the hidden generator subcommand. README.md is untouched.</done>
</task>

</tasks>

<verification>
- `.goreleaser.yaml` parses as valid YAML
- `.goreleaser.yaml` references both contrib filenames in both `archives.files` and `brews.install`
- `INSTALL.md` has a `## Shell Completions` section with three subsections
- `INSTALL.md` does not mention the hidden `completion` subcommand
- `README.md` is unchanged compared to the previous commit
- If `goreleaser` is available locally: `goreleaser check` exits 0
</verification>

<success_criteria>
- Release tarballs will contain the two completion files
- Homebrew formula will install them into the correct fpath locations
- INSTALL.md documents both install paths (Homebrew automatic, manual script)
- README.md is intentionally not touched (D-09)
- The hidden `completion` subcommand stays hidden — it is not mentioned in user-facing docs
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-phase-autosuggestion/10-05-SUMMARY.md`.
</output>

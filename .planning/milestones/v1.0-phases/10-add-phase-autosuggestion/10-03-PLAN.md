---
phase: 10-add-phase-autosuggestion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/docker-deploy/main.go
  - cmd/docker-deploy/main_test.go
  - internal/completion/completion.go
  - internal/completion/completion_test.go
autonomous: true
gap_closure: true
requirements: []
tags:
  - completion
  - cobra
  - cli-plugin

must_haves:
  truths:
    - "The `completion [bash|zsh]` subcommand exists and is hidden from `docker deploy --help`"
    - "Running `docker-deploy completion bash` writes a valid bash completion script to stdout"
    - "Running `docker-deploy completion zsh` writes a valid zsh completion script to stdout"
    - "No dynamic flag completion hooks (RegisterFlagCompletionFunc) are registered on the deploy command"
    - "internal/completion package contains only bash.go and zsh.go (plus their tests); completion.go is deleted"
    - "Generated bash/zsh scripts complete only static flag/subcommand names — no value suggestions from deploy.yaml or ~/.ssh/config"
    - "`go build ./...` succeeds and `go test ./...` passes"
  artifacts:
    - path: "cmd/docker-deploy/main.go"
      provides: "buildCompletionCmd() returns Hidden cobra.Command; no completion.Register() call"
      contains: "Hidden:"
    - path: "internal/completion/bash.go"
      provides: "GenerateBash kept unchanged"
    - path: "internal/completion/zsh.go"
      provides: "GenerateZsh kept unchanged"
  key_links:
    - from: "cmd/docker-deploy/main.go"
      to: "internal/completion (bash.go, zsh.go)"
      via: "completion.GenerateBash/GenerateZsh"
      pattern: "completion\\.Generate(Bash|Zsh)"
    - from: "cmd/docker-deploy/main.go"
      to: "(no longer imports) internal/sshconfig via completion"
      via: "import removal — completion package no longer depends on sshconfig or config"
      pattern: "internal/sshconfig"
---

<objective>
Replace the dynamic-completion implementation in the docker-deploy binary with a static cobra-generated approach.

Purpose: Per CONTEXT.md D-02 / D-03 / D-04, dynamic per-flag completions are being removed. The `completion` subcommand stays (for generating static scripts) but is marked Hidden. The completion package shrinks to just GenerateBash/GenerateZsh wrappers.

Output:
- `cmd/docker-deploy/main.go` with `buildCompletionCmd()` returning `Hidden: true, DisableFlagsInUseLine: true`
- `cmd/docker-deploy/main.go` with the `completion.Register(cmd)` line removed
- `internal/completion/completion.go` deleted (HostCompletionFunc, PathCompletionFunc, ComposeFileCompletionFunc, dedupStrings, Register)
- `internal/completion/completion_test.go` deleted (tests for deleted funcs)
- `cmd/docker-deploy/main_test.go` updated: `TestCompletionCmd_Registered` adjusted to also assert the subcommand has `Hidden == true`
- Build passes; all tests pass; `internal/completion` package depends only on `github.com/spf13/cobra` (no `internal/sshconfig` / `internal/config` imports)
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-add-phase-autosuggestion/10-CONTEXT.md
@.planning/notes/completion-rework-design.md
@cmd/docker-deploy/main.go
@cmd/docker-deploy/main_test.go
@internal/completion/completion.go
@internal/completion/completion_test.go
@internal/completion/bash.go
@internal/completion/zsh.go

<interfaces>
<!-- Key cobra completion APIs the executor will use -->

From github.com/spf13/cobra (Command struct fields):
```go
type Command struct {
    Use                   string
    Short                 string
    Hidden                bool       // hides from help output
    DisableFlagsInUseLine bool       // suppresses [flags] in Use line
    ValidArgs             []string
    Args                  PositionalArgs
    SilenceUsage          bool
    RunE                  func(cmd *Command, args []string) error
}
```

From internal/completion/bash.go:
```go
func GenerateBash(cmd *cobra.Command, w io.Writer) error
```

From internal/completion/zsh.go:
```go
func GenerateZsh(cmd *cobra.Command, w io.Writer) error
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dynamic completion code and its tests</name>
  <files>internal/completion/completion.go, internal/completion/completion_test.go</files>
  <read_first>
    - internal/completion/completion.go (current content — to confirm what is being deleted)
    - internal/completion/completion_test.go (current content — to confirm test coverage being removed)
    - .planning/phases/10-add-phase-autosuggestion/10-CONTEXT.md (D-03, D-04 enforce these deletions)
  </read_first>
  <action>
    Delete the file `internal/completion/completion.go` entirely (per D-03). This removes:
    - `Register(cmd *cobra.Command)`
    - `HostCompletionFunc`
    - `PathCompletionFunc`
    - `ComposeFileCompletionFunc`
    - `dedupStrings(in []string) []cobra.Completion`
    - imports of `os`, `path/filepath`, `github.com/webcane/docker-deploy/internal/config`, `github.com/webcane/docker-deploy/internal/sshconfig`

    Delete the file `internal/completion/completion_test.go` entirely. It exercises only the symbols listed above; once `completion.go` is gone, this test file will not compile.

    Do NOT modify `internal/completion/bash.go`, `internal/completion/zsh.go`, `internal/completion/bash_test.go`, or `internal/completion/zsh_test.go` — these stay as-is per D-04.

    Use `rm` (or equivalent file delete) — not Edit-to-empty-file. The files must not exist after this task.
  </action>
  <verify>
    <automated>test ! -f internal/completion/completion.go && test ! -f internal/completion/completion_test.go && test -f internal/completion/bash.go && test -f internal/completion/zsh.go</automated>
  </verify>
  <acceptance_criteria>
    - `internal/completion/completion.go` does not exist on disk
    - `internal/completion/completion_test.go` does not exist on disk
    - `internal/completion/bash.go` still exists and is unmodified
    - `internal/completion/zsh.go` still exists and is unmodified
    - `internal/completion/bash_test.go` and `internal/completion/zsh_test.go` still exist and are unmodified
    - `grep -R "HostCompletionFunc\|PathCompletionFunc\|ComposeFileCompletionFunc\|dedupStrings" internal/ cmd/` returns no matches
  </acceptance_criteria>
  <done>The dynamic completion source and its tests are removed; only the bash/zsh generator files remain in the package.</done>
</task>

<task type="auto">
  <name>Task 2: Remove completion.Register call and hide the completion subcommand in main.go</name>
  <files>cmd/docker-deploy/main.go</files>
  <read_first>
    - cmd/docker-deploy/main.go (lines 1-175 — see buildDeployCmd at L53 and buildCompletionCmd at L154)
    - .planning/phases/10-add-phase-autosuggestion/10-CONTEXT.md (D-02 — Hidden, DisableFlagsInUseLine; D-03 — drop Register call)
  </read_first>
  <action>
    In `cmd/docker-deploy/main.go`:

    1) In `buildDeployCmd()` (around line 92), DELETE the line:
       ```
       completion.Register(cmd)
       ```
       Do not leave a blank stub. The `completion` package is still imported (still used for GenerateBash/GenerateZsh in `buildCompletionCmd`), so the `"github.com/webcane/docker-deploy/internal/completion"` import stays.

    2) In `buildCompletionCmd()` (around line 154), modify the returned `*cobra.Command` so that, in addition to the existing fields (`Use`, `Short`, `ValidArgs`, `Args`, `SilenceUsage`, `RunE`), it also sets:
       - `Hidden: true`
       - `DisableFlagsInUseLine: true`

       The exact `Use` string MUST remain `"completion [bash|zsh]"` (do not change — `cmd/docker-deploy/main_test.go::TestCompletionCmd_Registered` matches on this exact literal).

       Update the doc comment above `buildCompletionCmd` to mention that the subcommand is hidden and is intended only for `make completions` / release generation (not for end users); reference decisions D-02 and D-03.

    3) Verify the final file: the only references to `completion.` should be `completion.GenerateBash(cmd, os.Stdout)` and `completion.GenerateZsh(cmd, os.Stdout)` inside `buildCompletionCmd`. No `completion.Register`, no `completion.HostCompletionFunc`, etc.
  </action>
  <verify>
    <automated>grep -c 'completion\.Register' cmd/docker-deploy/main.go | grep -v '^#' | grep -q '^0$' && grep -q 'Hidden:\s*true' cmd/docker-deploy/main.go && grep -q 'DisableFlagsInUseLine:\s*true' cmd/docker-deploy/main.go && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'completion\.Register' cmd/docker-deploy/main.go` returns no matches
    - `buildCompletionCmd` returns a `*cobra.Command` literal containing both `Hidden: true` and `DisableFlagsInUseLine: true`
    - The string literal `"completion [bash|zsh]"` is still present in `buildCompletionCmd` (the Use field is unchanged)
    - `completion.GenerateBash` and `completion.GenerateZsh` are still called inside `buildCompletionCmd`'s RunE
    - `go build ./...` exits 0
    - Running the built binary, `docker-deploy --help` does NOT list `completion` (verified by `./bin/docker-deploy --help 2>&1 | grep -v '^#' | grep -c '^  completion'` printing `0`)
    - Running `./bin/docker-deploy completion bash | head -1` still produces output beginning with `# bash completion V2 for docker-deploy` (or the equivalent first line cobra emits) — the subcommand still works, it is just hidden
  </acceptance_criteria>
  <done>main.go no longer registers dynamic completions and the completion subcommand is marked Hidden + DisableFlagsInUseLine while remaining invokable.</done>
</task>

<task type="auto">
  <name>Task 3: Update TestCompletionCmd_Registered to assert Hidden==true and ensure all tests pass</name>
  <files>cmd/docker-deploy/main_test.go</files>
  <read_first>
    - cmd/docker-deploy/main_test.go (lines 410-420 — TestCompletionCmd_Registered)
    - cmd/docker-deploy/main.go (to confirm the new Hidden field is set in buildCompletionCmd)
  </read_first>
  <action>
    In `cmd/docker-deploy/main_test.go`, update `TestCompletionCmd_Registered` (currently around lines 410-420):

    - Keep the existing iteration that locates the subcommand whose `Use` equals `"completion [bash|zsh]"`.
    - Inside the matching branch, before `return`, add an assertion: if `sub.Hidden != true`, call `t.Errorf("completion subcommand Hidden = %v; want true (D-02)", sub.Hidden)`.
    - Also assert `sub.DisableFlagsInUseLine == true` with an analogous `t.Errorf` message referencing D-02.
    - Update the doc comment above the test to reference D-02 in addition to D-04, D-05.

    Do not change any other tests. Do not introduce new helper imports beyond what is already imported (the test already has `testing` and uses `cmd.Commands()` from cobra via buildDeployCmd's return value).

    After editing, run `go test ./...` from the repository root. All packages must pass. If any test was relying on the deleted `completion.HostCompletionFunc` / etc. (it should not, after Task 1, since that test file was also deleted), surface the failure — do not paper over it.
  </action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `TestCompletionCmd_Registered` source contains a check for `sub.Hidden` (matching `sub\.Hidden`)
    - `TestCompletionCmd_Registered` source contains a check for `sub.DisableFlagsInUseLine` (matching `sub\.DisableFlagsInUseLine`)
    - `go test ./cmd/docker-deploy/... ./internal/completion/...` exits 0
    - `go test ./...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <done>The completion-subcommand test now enforces the Hidden+DisableFlagsInUseLine contract from D-02, and the full test suite passes.</done>
</task>

</tasks>

<verification>
- `go build ./...` succeeds
- `go test ./...` passes
- `go vet ./...` passes
- `grep -R "HostCompletionFunc\|PathCompletionFunc\|ComposeFileCompletionFunc\|completion\.Register" cmd/ internal/` returns no matches
- `internal/completion/` contains only: bash.go, bash_test.go, zsh.go, zsh_test.go
- `./bin/docker-deploy --help` does NOT list `completion` as a subcommand (hidden)
- `./bin/docker-deploy completion bash | head -1` still emits a cobra-generated script header
- `./bin/docker-deploy completion zsh | head -1` still emits `#compdef docker-deploy` (or cobra's zsh header)
</verification>

<success_criteria>
- Dynamic completion code (`completion.go` + its test file) is deleted
- `completion.Register(cmd)` call is gone from `cmd/docker-deploy/main.go`
- `buildCompletionCmd` sets `Hidden: true` and `DisableFlagsInUseLine: true`
- `TestCompletionCmd_Registered` enforces both new fields
- All tests and builds pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-phase-autosuggestion/10-03-SUMMARY.md`.
</output>

---
phase: 10-add-phase-autosuggestion
plan: "05"
subsystem: release
tags: [goreleaser, homebrew, documentation, completion, bash, zsh]

# Dependency graph
requires:
  - phase: 10-add-phase-autosuggestion
    plan: "03"
    provides: "Hidden completion subcommand; internal/completion package with GenerateBash/GenerateZsh"
  - phase: 10-add-phase-autosuggestion
    plan: "04"
    provides: "contrib/_docker-deploy and contrib/docker-deploy.bash generated files; contrib/install-completions.sh"

provides:
  - ".goreleaser.yaml archives[].files includes contrib/_docker-deploy and contrib/docker-deploy.bash"
  - ".goreleaser.yaml brews[0].install installs completion files to share/zsh/site-functions and share/bash-completion/completions"
  - "INSTALL.md ## Shell Completions section with Homebrew, manual, and verify subsections"

affects:
  - "Release pipeline — tarballs now contain completion files"
  - "Homebrew formula — brew install now installs completions automatically"
  - "User documentation — INSTALL.md documents both install paths"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "goreleaser archives[].files with src: entries adds extra files to per-arch tarballs (not release.extra_files which only attaches to GitHub release page)"
    - "Homebrew brews.install Ruby block can reference files from the tarball by basename"

key-files:
  created:
    - ".planning/phases/10-add-phase-autosuggestion/10-05-SUMMARY.md"
  modified:
    - ".goreleaser.yaml"
    - "INSTALL.md"

key-decisions:
  - "D-06: Goreleaser archives[].files (not release.extra_files) is correct for including files in per-arch tarballs that the Homebrew formula extracts"
  - "D-07: brews[0].install extended with zsh/site-functions and bash-completion/completions lines — zero user config on Homebrew macOS"
  - "D-09: README.md intentionally not touched; hidden completion subcommand not mentioned in INSTALL.md"

# Metrics
duration: 2min
completed: 2026-06-02
---

# Phase 10 Plan 05: Goreleaser and INSTALL.md Completion Rollout Summary

**Wired completion files into release tarballs and Homebrew formula via goreleaser archives[].files and brews.install; added Shell Completions section to INSTALL.md covering Homebrew (automatic) and manual install-completions.sh paths**

## Performance

- **Duration:** 2 min
- **Started:** 2026-06-02T09:37:49Z
- **Completed:** 2026-06-02T09:39:52Z
- **Tasks:** 2
- **Files modified:** 2 modified

## Accomplishments

- Updated `.goreleaser.yaml` archives entry with `files:` listing `contrib/_docker-deploy` and `contrib/docker-deploy.bash` as `src:` entries — these are now bundled into every release tarball
- Extended `brews[0].install` in `.goreleaser.yaml` with two Ruby lines that install the completion files into Homebrew's standard `share/zsh/site-functions/` and `share/bash-completion/completions/` directories
- Appended `## Shell Completions` section to `INSTALL.md` with three subsections: Homebrew (automatic), Manual install (non-Homebrew), and Verify
- All acceptance criteria verified: valid YAML, correct goreleaser structure, correct INSTALL.md content, hidden subcommand not mentioned, README.md untouched

## Task Commits

Each task was committed atomically:

1. **Task 1: Add contrib files to release tarballs and update brews.install** - `17b925b` (feat)
2. **Task 2: Add Shell Completions section to INSTALL.md** - `1aa6d97` (docs)

## Files Created/Modified

- `.goreleaser.yaml` - Added `files:` list under `archives[0]` with `contrib/_docker-deploy` and `contrib/docker-deploy.bash`; extended `brews[0].install` with two completion install lines per D-06 and D-07
- `INSTALL.md` - Appended `## Shell Completions` section (70 lines) with Homebrew, manual, and verify subsections; existing sections untouched; README.md not modified per D-09

## Decisions Made

- D-06 applied: Used `archives[].files` with `src:` entries (not `release.extra_files`) — per plan interface note, `archives[].files` adds files to per-arch tarballs while `release.extra_files` only attaches to the GitHub release page
- D-07 applied: `brews[0].install` now installs `_docker-deploy` to `share/zsh/site-functions` and `docker-deploy.bash` to `share/bash-completion/completions`
- D-09 applied: `README.md` not modified; hidden `completion` subcommand not referenced anywhere in `INSTALL.md`

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None - no stubs. The goreleaser configuration references `contrib/_docker-deploy` and `contrib/docker-deploy.bash` which are generated by plan 10-04 (`make completions`). These files must be generated and committed before a goreleaser release is triggered.

## Threat Flags

None - no new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

- `.goreleaser.yaml` modified: FOUND (git log shows 17b925b)
- `INSTALL.md` modified: FOUND (git log shows 1aa6d97)
- `## Shell Completions` section: FOUND in INSTALL.md
- goreleaser `files:` block: FOUND in .goreleaser.yaml
- goreleaser `brews[0].install` completion lines: FOUND in .goreleaser.yaml
- README.md: unchanged (git diff shows no modifications)
- Hidden subcommand: not mentioned in INSTALL.md

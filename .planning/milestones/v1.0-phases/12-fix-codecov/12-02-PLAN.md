---
phase: "12"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "README 'What is docker-deploy?' paragraph is 100 words or fewer and states who the tool is for and why it is simpler than CI/CD"
    - "README Installation section shows only the install script as the primary method"
    - "README Installation section contains a link to INSTALL.md for other install methods"
    - "README does not contain the Homebrew, Manual binary, or go install command blocks"
  artifacts:
    - path: "README.md"
      provides: "Restructured value prop and install section"
      contains: "INSTALL.md"
  key_links:
    - from: "README.md ## Installation"
      to: "INSTALL.md"
      via: "relative markdown link [INSTALL.md](INSTALL.md)"
      pattern: "\\[INSTALL\\.md\\]\\(INSTALL\\.md\\)"
---

<objective>
Edit README.md to: (1) tighten the "What is docker-deploy?" value proposition to 100 words or fewer, and (2) restructure the Installation section to show only the install script as the primary method, with a one-line note linking to INSTALL.md for the three remaining methods.

This plan edits README.md only. INSTALL.md is created in plan 12-03 (also Wave 1 — independent).

Purpose: README Install section is currently four equally-weighted options; per D-04 the install script is primary. Per D-05 the other methods move to INSTALL.md entirely so the README stays focused.
Output: README.md with concise value prop and simplified Install section that links to INSTALL.md.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/12-fix-codecov/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten value proposition paragraph</name>
  <files>README.md</files>
  <read_first>
    - README.md (read lines 1–70 to see the full "What is docker-deploy?" section and the badge header)
  </read_first>
  <action>
    Rewrite the body of the "## What is docker-deploy?" section (the paragraph starting "docker-deploy is a Docker CLI plugin...") so that it:

    - Is 100 words or fewer (count words after writing — cut until the limit is met)
    - States who the tool is for: solo developers or small teams with a single VPS
    - States the core value: single command copies compose project files to a remote host via SFTP and runs `docker compose up -d`
    - States why it is simpler: no git on remote, no container registry, no CI/CD pipeline required
    - Does NOT add new information not present in the current paragraph

    Do not change the section heading "## What is docker-deploy?" or anything outside this paragraph.
  </action>
  <verify>
    <automated>awk '/^## What is docker-deploy\?/,/^## /' README.md | grep -v "^## " | wc -w</automated>
  </verify>
  <acceptance_criteria>
    - Word count of the "What is docker-deploy?" section body is 100 or fewer (command above returns ≤ 100)
    - The paragraph contains the phrase "remote host" or "remote VPS" (not removed entirely)
    - The paragraph contains the words "single command" or equivalent phrasing about simplicity
    - The paragraph contains a reference to no git, no registry, or no CI/CD
    - The section heading "## What is docker-deploy?" is unchanged
  </acceptance_criteria>
  <done>Value proposition paragraph is 100 words or fewer and clearly conveys who the tool is for and why it is simpler than CI/CD pipelines.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure Installation section — install script primary + INSTALL.md link</name>
  <files>README.md</files>
  <read_first>
    - README.md (read lines 11–65 to see the complete current Installation section with all four options)
  </read_first>
  <action>
    Replace the entire "## Installation" section (from "## Installation" through the end of the "Option 4: go install" block) with the following structure:

    1. Keep the "## Installation" heading unchanged.
    2. Add a single subsection "### Install script" (no "Option N:" prefix, per D-06) containing:
       - The brief introductory line "Pin to a release tag (recommended...)" or equivalent
       - The two curl command blocks that are currently under "Option 2: Install script"
    3. After the install script block, add a single paragraph:
       "For Homebrew, manual binary download, and `go install`, see [INSTALL.md](INSTALL.md)."
    4. Remove the Homebrew block ("### Option 1: Homebrew"), the Manual binary block ("### Option 3: Manual binary"), and the go install block ("### Option 4: go install") from README.md entirely — they move to INSTALL.md (plan 12-03).

    The resulting Installation section must contain only: the "## Installation" heading, one "### Install script" subsection with the curl command blocks, and the INSTALL.md link paragraph. Nothing else.
  </action>
  <verify>
    <automated>grep -c "INSTALL.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "INSTALL.md" README.md` returns at least 1
    - `grep -c "Option 1\|Option 2\|Option 3\|Option 4" README.md` returns 0 (all "Option N:" prefixes removed)
    - `grep -c "brew tap\|brew install" README.md` returns 0 (Homebrew block removed)
    - `grep -c "go install" README.md` returns 0 (go install block removed)
    - `grep -c "Manual binary\|releases page" README.md` returns 0 (Manual binary block removed)
    - `grep -c "curl -fsSL" README.md` returns 2 (the two install script curl commands remain)
    - `grep -c "### Install script" README.md` returns 1
    - The link `[INSTALL.md](INSTALL.md)` is present in README.md
  </acceptance_criteria>
  <done>README Installation section contains only the install script block and a one-line link to INSTALL.md; all other install methods removed from README.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source edit | Documentation-only edit; no code or config changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Information Disclosure | README.md install section | accept | Removing install blocks from README does not remove the information — it moves to INSTALL.md; no security surface |
</threat_model>

<verification>
Run all grep checks listed in acceptance_criteria for Task 2.
Manually confirm the "What is docker-deploy?" word count is ≤ 100.
Confirm `[INSTALL.md](INSTALL.md)` link is present in the Installation section.
</verification>

<success_criteria>
- README.md value proposition is 100 words or fewer
- README.md Installation section has exactly one install method (install script) and a link to INSTALL.md
- Zero occurrences of "Option 1:", "Option 2:", "Option 3:", "Option 4:" remain in README.md
- `[INSTALL.md](INSTALL.md)` link present
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-codecov/12-02-SUMMARY.md`
</output>

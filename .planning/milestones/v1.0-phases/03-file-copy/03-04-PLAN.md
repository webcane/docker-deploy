---
phase: 03-file-copy
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - internal/filetransfer/upload.go
  - cmd/docker-deploy/main.go
autonomous: true
requirements: [DEPLOY-02, DEPLOY-03, FILES-01, FILES-02, FILES-03]

must_haves:
  truths:
    - "shellQuote escapes embedded single quotes so paths like /opt/foo'bar cannot break shell quoting context"
    - "main.go existence check uses shellQuote (not inline single-quote interpolation) for resolved.Path"
    - "First deploy into /opt (target absent) succeeds: staging dir is moved to remoteBase with sudo"
    - "Repeat deploy into /opt succeeds: existing remoteBase is backed up and replaced with sudo mv ops"
    - "If atomic swap step 2 fails, step 1 is rolled back (remoteBase restored from backup)"
    - "On any swap failure, error message includes the backup directory path for manual recovery"
    - "sudo password is collected at most once per deploy invocation (single interactive prompt)"
    - "Ownership of remoteBase is set to the connecting user (chown) before file operations"
  artifacts:
    - path: "internal/filetransfer/upload.go"
      provides: "Fixed shellQuote, single-sudo-invocation for mkdir+chown+chmod, sudo mv for atomic swap, rollback on step-2 failure"
      contains: "strings.ReplaceAll"
    - path: "cmd/docker-deploy/main.go"
      provides: "shellQuote used for resolved.Path in existence check command"
      contains: "shellQuote"
  key_links:
    - from: "Upload() sudo section"
      to: "remoteBase ownership"
      via: "single sh -c pipeline: mkdir -p && chown && chmod"
      pattern: "sudo.*chown.*chmod"
    - from: "atomic swap"
      to: "rollback"
      via: "step-2 failure triggers mv old remoteBase"
      pattern: "oldDir.*remoteBase"
---

<objective>
Fix four gaps found during human UAT and code review of Phase 3. The gaps are:

1. Gap 1 — Ownership setup: /opt/ is root-owned; the connecting user needs mkdir+chown+chmod run via sudo before any mv operations can succeed.
2. Gap 2 — Atomic mv needs sudo: all mv/rm operations against /opt/ must run with sudo.
3. Gap 3 (CR-01) — shellQuote command injection: embedded single quotes in paths break shell quoting; same flaw in main.go:179.
4. Gap 4 (CR-02) — No rollback on atomic swap step-2 failure: remoteBase goes absent if mv stagingDir remoteBase fails after mv remoteBase old has already succeeded.

Purpose: Phase 3 cannot be considered complete while a real deploy against /opt/ fails and a command-injection path exists.
Output: Fixed upload.go and main.go; all four gaps closed; existing tests still pass.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/03-file-copy/03-VERIFICATION.md

<interfaces>
<!-- Extracted from internal/filetransfer/upload.go — current state before changes -->

func Upload(ctx context.Context, client *gossh.Client, localDir, remoteBase string, excludes []string) error
func shellQuote(s string) string  // line 252: DOES NOT escape embedded single quotes — fix required
func sshExec(client *gossh.Client, cmd string) error
func sshExecOutput(client *gossh.Client, cmd string) (string, error)
func remoteExists(client *gossh.Client, remotePath string) (bool, error)

// Current shellQuote (BROKEN — line 252):
//   return "'" + s + "'"
//
// Fixed shellQuote:
//   return "'" + strings.ReplaceAll(s, "'", "'\\''") + "'"
//
// Current atomic swap (lines 184-198) — no sudo, no rollback:
//   mv remoteBase oldDir        (step 1)
//   mv stagingDir remoteBase    (step 2 — no rollback if this fails)
//   rm -rf oldDir               (step 3)
//
// Current sudo section (lines 126-166) — only covers mkdir, not chown/chmod/mv:
//   echo <pw> | sudo -S -p '' sh -c 'mkdir -p <dir> && chown $(id -un):$(id -gn) <dir>'
//   (missing: chmod 755; missing: sudo for mv ops)

// From cmd/docker-deploy/main.go line 179 (BROKEN — inline interpolation):
//   out, err := session.Output(fmt.Sprintf("test -d '%s' && echo exists || echo absent", resolved.Path))
//
// Fixed (must use shellQuote exported from filetransfer, or duplicate the fix inline):
//   out, err := session.Output(fmt.Sprintf("test -d %s && echo exists || echo absent", filetransfer.ShellQuote(resolved.Path)))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix shellQuote and export it; fix main.go existence check</name>
  <files>internal/filetransfer/upload.go, cmd/docker-deploy/main.go</files>
  <read_first>
    - internal/filetransfer/upload.go (full file — understand current shellQuote at line 252 and all call sites)
    - cmd/docker-deploy/main.go (lines 170-196 — the existence check at line 179)
    - internal/filetransfer/filter_test.go (to understand test file conventions for the package)
  </read_first>
  <behavior>
    - shellQuote("/opt/normal") == "'/opt/normal'"
    - shellQuote("/opt/foo'bar") == "'/opt/foo'\\''bar'"
    - shellQuote("") == "''"
    - shellQuote("it's a test") == "'it'\\''s a test'"
  </behavior>
  <action>
    In internal/filetransfer/upload.go:

    1. Add "strings" to the import block (it is not currently imported).

    2. Rename shellQuote to ShellQuote (exported). Update all call sites within upload.go to use ShellQuote. The call sites are:
       - line 118: shellQuote(stagingDir) in rm -rf cleanup
       - line 124: shellQuote(remoteBase) in mkdir -p attempt
       - line 136-138: shellQuote(string(pw)), shellQuote(remoteBase) x2 in sudo cmd
       - line 206: shellQuote(remotePath) in remoteExists
       - line 184: shellQuote(remoteBase), shellQuote(oldDir) in mv commands (these will be replaced in Task 2, but still update the function name now)
       - line 195: shellQuote(stagingDir), shellQuote(remoteBase) in first-deploy mv

    3. Replace the body of ShellQuote (was at line 252) with:
       return "'" + strings.ReplaceAll(s, "'", "'\\''") + "'"

    4. Add a test file internal/filetransfer/shellquote_test.go with table-driven tests for ShellQuote. Test cases (at minimum):
       - input "" -> output "''"
       - input "/opt/normal" -> output "'/opt/normal'"
       - input "/opt/foo'bar" -> output "'/opt/foo'\\''bar'"
       - input "it's a test" -> output "'it'\\''s a test'"
       Run: go test ./internal/filetransfer/ -v -run TestShellQuote

    In cmd/docker-deploy/main.go:

    5. Add import for filetransfer package if not already present (it is imported at line 18 as `filetransfer "github.com/mniedre/docker-deploy/internal/filetransfer"`).

    6. At line 179, replace:
       session.Output(fmt.Sprintf("test -d '%s' && echo exists || echo absent", resolved.Path))
       with:
       session.Output(fmt.Sprintf("test -d %s && echo exists || echo absent", filetransfer.ShellQuote(resolved.Path)))

    Do NOT change any other logic in main.go. The fmt.Sprintf format string must not wrap the ShellQuote result in additional quotes — ShellQuote already produces the quoted string.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/docker-deploy && go test ./internal/filetransfer/ -v -run TestShellQuote && go build ./... && go vet ./...</automated>
  </verify>
  <done>
    - TestShellQuote passes all cases including the embedded-single-quote case
    - go build ./... exits 0 (ShellQuote export is visible to main.go)
    - go vet ./... exits 0
    - grep -n "shellQuote" internal/filetransfer/upload.go returns 0 results (all renamed to ShellQuote)
    - grep -n "ShellQuote" cmd/docker-deploy/main.go shows line ~179 using ShellQuote(resolved.Path)
    - grep -n "strings.ReplaceAll" internal/filetransfer/upload.go shows the fix in the ShellQuote body
  </done>
</task>

<task type="auto">
  <name>Task 2: Sudo ownership setup + sudo atomic mv + rollback on step-2 failure</name>
  <files>internal/filetransfer/upload.go</files>
  <read_first>
    - internal/filetransfer/upload.go (full file — understand the Upload() function flow after Task 1 changes; focus on lines 122-200: the sudo section and atomic swap section)
  </read_first>
  <action>
    Rewrite the sudo section (currently lines 126-167) and atomic swap section (lines 176-200) in Upload(). All changes are inside the Upload() function body. Do not change function signatures, imports (beyond "strings" already added in Task 1), or any logic outside Upload().

    SECTION A — Sudo section rewrite (replaces lines 122-167):

    Declare a package-level variable `sudoPw string` — do NOT do this. Instead, declare `sudoPw` as a local variable inside Upload() before the mkdir step. This variable persists across the mkdir, chown/chmod, and mv calls within a single Upload() invocation.

    The sudo section currently starts at Step 8 (line 122). Replace it entirely with:

    ```
    // Step 8: Ensure target directory exists and is owned by the connecting user.
    // Attempt 1 — without sudo (works if user already owns /opt/<project>).
    sudoPw := ""
    mkdirOK := sshExec(client, fmt.Sprintf("mkdir -p %s", ShellQuote(remoteBase))) == nil

    if !mkdirOK {
        // Attempt 2 — interactive sudo: collect password (up to 3 attempts).
        // Single sh -c pipeline: mkdir + chown + chmod to avoid multiple prompts.
        sudoOK := false
        for attempt := 1; attempt <= 3; attempt++ {
            fmt.Fprintf(os.Stderr, "[sudo] password for remote host: ")
            pw, err := term.ReadPassword(int(syscall.Stdin))
            fmt.Fprintln(os.Stderr)
            if err != nil {
                break
            }
            pwStr := string(pw)
            sudoCmd := fmt.Sprintf(
                "echo %s | sudo -S -p '' sh -c 'mkdir -p %s && chown $(id -un):$(id -gn) %s && chmod 755 %s'",
                ShellQuote(pwStr), ShellQuote(remoteBase), ShellQuote(remoteBase), ShellQuote(remoteBase),
            )
            if err := sshExec(client, sudoCmd); err == nil {
                sudoPw = pwStr
                sudoOK = true
                break
            }
            if attempt < 3 {
                fmt.Fprintln(os.Stderr, "Sorry, try again.")
            }
        }

        if !sudoOK {
            remoteHost := client.RemoteAddr().String()
            fmt.Fprintf(os.Stderr,
                "Warning: could not create target directory %s.\n"+
                    "Uploaded files are staged at %s on the remote server.\n"+
                    "To deploy manually:\n"+
                    "  ssh %s 'sudo mv %s %s'\n"+
                    "Or re-run after granting access:\n"+
                    "  ssh %s 'sudo mkdir -p %s && sudo chown <user> %s'\n",
                remoteBase,
                stagingDir,
                remoteHost, stagingDir, remoteBase,
                remoteHost, remoteBase, remoteBase,
            )
            return fmt.Errorf("could not create target directory %s", remoteBase)
        }
    }
    ```

    Key changes from the original:
    - Added `chmod 755 %s` to the single sh -c pipeline (Gap 1 fix: ensure user has write permission on the directory).
    - `sudoPw` is captured when sudo succeeds so it can be reused in the mv step below without re-prompting.

    SECTION B — Atomic swap rewrite (replaces lines 176-200):

    The `remoteExists` check at Step 9 is unchanged. Replace Step 10 (the if/else atomic swap block) entirely with:

    ```
    // Step 10: Atomic swap via SSH exec.
    // All mv/rm operations against /opt use sudo when sudoPw was needed for mkdir.
    // sudoRun builds the sudo-prefixed command if sudoPw is non-empty.
    sudoRun := func(cmd string) error {
        if sudoPw == "" {
            return sshExec(client, cmd)
        }
        return sshExec(client, fmt.Sprintf("echo %s | sudo -S -p '' sh -c %s", ShellQuote(sudoPw), ShellQuote(cmd)))
    }

    if exists {
        // Four-step atomic swap:
        // 1. Move staging to /opt space as remoteBase-new-<ts>  (always writable: staging is in /tmp)
        // 2. Backup existing remoteBase to remoteBase-old-<ts>
        // 3. Move remoteBase-new-<ts> to remoteBase             (place new)
        // 4. Remove backup                                       (clean up)
        newDir := remoteBase + "-new-" + timestamp
        oldDir := remoteBase + "-old-" + timestamp

        // Step 10.1: move staging into /opt space
        if err := sudoRun(fmt.Sprintf("mv %s %s", ShellQuote(stagingDir), ShellQuote(newDir))); err != nil {
            return fmt.Errorf("moving staging dir into target space: %w", err)
        }
        // Step 10.2: backup existing target
        if err := sudoRun(fmt.Sprintf("mv %s %s", ShellQuote(remoteBase), ShellQuote(oldDir))); err != nil {
            // Rollback step 10.1
            _ = sudoRun(fmt.Sprintf("mv %s %s", ShellQuote(newDir), ShellQuote(stagingDir)))
            return fmt.Errorf("renaming existing target to backup: %w", err)
        }
        // Step 10.3: place new version
        if err := sudoRun(fmt.Sprintf("mv %s %s", ShellQuote(newDir), ShellQuote(remoteBase))); err != nil {
            // Rollback step 10.2: restore remoteBase from backup
            _ = sudoRun(fmt.Sprintf("mv %s %s", ShellQuote(oldDir), ShellQuote(remoteBase)))
            return fmt.Errorf("renaming staging dir to target: backup is at %s: %w", oldDir, err)
        }
        // Step 10.4: clean up backup (failure is non-fatal — deployment succeeded)
        if err := sudoRun(fmt.Sprintf("rm -rf %s", ShellQuote(oldDir))); err != nil {
            fmt.Fprintf(os.Stderr, "Warning: could not remove backup directory %s: %v\n", oldDir, err)
        }
    } else {
        // First deploy: move staging dir directly to target (needs sudo if under /opt).
        if err := sudoRun(fmt.Sprintf("mv %s %s", ShellQuote(stagingDir), ShellQuote(remoteBase))); err != nil {
            return fmt.Errorf("moving staging dir to target: %w", err)
        }
    }
    ```

    Key changes from the original:
    - `sudoRun` closure: routes through sudo when `sudoPw != ""`, bare sshExec otherwise. Avoids re-prompting.
    - Four-step swap instead of three: staging goes to `-new-<ts>` first (step 10.1), then existing is backed up (step 10.2), then new replaces target (step 10.3), then backup cleaned (step 10.4).
    - Rollback on step-10.2 failure: moves newDir back to stagingDir (Gap 4 partial rollback).
    - Rollback on step-10.3 failure: moves oldDir back to remoteBase AND includes oldDir path in error message (Gap 4 full rollback).
    - Step 10.4 (rm backup) is non-fatal: prints warning to stderr but does not return error (fixes WR-03 anti-pattern from VERIFICATION.md).
    - First-deploy path also uses sudoRun (Gap 2 fix: /tmp -> /opt mv needs sudo).

    After both sections are rewritten, the Upload() function must import: context, fmt, io, os, path, path/filepath, strings, syscall, time (same as before). Verify "strings" is imported (needed for ShellQuote added in Task 1).
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/docker-deploy && go build ./... && go vet ./... && go test ./internal/filetransfer/ -v && go test ./... </automated>
  </verify>
  <done>
    - go build ./... exits 0
    - go vet ./... exits 0
    - go test ./... exits 0 (all existing tests pass; no regressions)
    - grep -n "sudoRun" internal/filetransfer/upload.go shows the closure and its use in both the exists and first-deploy paths
    - grep -n "oldDir" internal/filetransfer/upload.go shows oldDir referenced in at minimum 2 error/rollback call sites
    - grep -n "chmod 755" internal/filetransfer/upload.go shows chmod in the sudo sh -c pipeline
    - grep -n "Warning: could not remove backup" internal/filetransfer/upload.go confirms backup-rm failure is non-fatal (Fprintf to Stderr, not return err)
    - grep -n '"-new-"' internal/filetransfer/upload.go shows the four-step swap pattern (newDir variable)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 fixed shellQuote to escape embedded single quotes and export it as ShellQuote. main.go:179 now uses ShellQuote(resolved.Path) instead of inline interpolation.
    Task 2 rewrote the sudo section to add chmod 755, capture sudoPw for reuse, and added a sudoRun closure so all mv/rm against /opt run via sudo. The atomic swap is now four steps with rollback on step-2 and step-3 failure. Backup-dir cleanup failure is non-fatal.
  </what-built>
  <how-to-verify>
    Run against a real SSH host (e.g., 192.168.1.99) where sshuser does NOT own /opt/:

    1. First deploy (target absent):
       cd /path/to/test-project && docker deploy --host ssh://sshuser@192.168.1.99
       Expected: "[sudo] password for remote host:" prompt appears once. After correct password, deploy completes without error. On remote: `ls /opt/test-deploy` shows project files; .git/ is absent.

    2. Repeat deploy:
       docker deploy --host ssh://sshuser@192.168.1.99
       Expected: "Target /opt/test-deploy exists on 192.168.1.99. Replace all contents? [y/N]" appears. Type "y". Deploy completes without error. No sudo prompt (non-interactive — sudoPw is unused on second run since directory now exists and is owned by sshuser).
       Note: if sshuser owns /opt/test-deploy from the first deploy, subsequent mv ops may not need sudo. If they do need sudo (parent /opt still requires it), a second prompt is expected. The key check is that the deploy succeeds.

    3. Verify no "renaming existing target to backup: running mv... Process exited with status 1" error.

    4. If you have a path with a single quote available (unlikely in practice), confirm: `docker deploy --host ssh://sshuser@192.168.1.99 --path /opt/test` still works (shellQuote no longer breaks on special characters).
  </how-to-verify>
  <resume-signal>Type "approved" if deploy succeeds end-to-end against the real host. Describe any remaining failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> shell command string | remoteBase and stagingDir are interpolated into SSH exec commands; must be safely quoted |
| sudoPw -> SSH command | sudo password travels as part of a shell command string; must be single-quoted with escaping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-CR01 | Tampering | ShellQuote in upload.go | mitigate | Replace embedded `'` with `'\''` before wrapping — implemented in Task 1 |
| T-03-CR01b | Tampering | main.go:179 inline interpolation | mitigate | Route resolved.Path through ShellQuote — implemented in Task 1 |
| T-03-PW01 | Information Disclosure | sudoPw in sudoRun closure | accept | Password held only in stack-local string within Upload() invocation; not logged, not written to disk; connection is SSH-encrypted in transit |
| T-03-PW02 | Tampering | echo pw | sudo -S | accept | sudo -S reads from stdin; echo is inside the SSH channel (encrypted); no local shell exposure. Alternative (sudo -S with stdin pipe per session) would require session.StdinPipe() complexity; risk is low given scope |
</threat_model>

<verification>
After both tasks complete:

1. go test ./... exits 0 — no regressions
2. go build ./... exits 0
3. go vet ./... exits 0
4. TestShellQuote passes including the /opt/foo'bar case
5. grep -rn "shellQuote" internal/ cmd/ returns 0 (all renamed to ShellQuote)
6. grep -n "strings.ReplaceAll" internal/filetransfer/upload.go returns the fix
7. grep -n "chmod 755" internal/filetransfer/upload.go returns a match
8. grep -n "oldDir" internal/filetransfer/upload.go returns rollback lines
9. grep -n "sudoRun" internal/filetransfer/upload.go returns closure + call sites
10. Human checkpoint: real deploy against 192.168.1.99 succeeds without "Process exited with status 1"
</verification>

<success_criteria>
- docker deploy against a root-owned /opt/ succeeds (no "mv ... Process exited with status 1")
- shellQuote/ShellQuote correctly escapes single quotes (TestShellQuote passes)
- main.go:179 uses ShellQuote — no more inline single-quote interpolation
- Atomic swap step-2 failure triggers rollback (remoteBase is never left absent)
- sudo password collected once per deploy; reused for all privileged ops
- All existing tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/03-file-copy/03-04-SUMMARY.md` with:
- What was fixed (all 4 gaps with one-line each)
- Files modified
- Test results (go test ./... output summary)
- Human checkpoint result
</output>

---
phase: 11-ci-tooling-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - codecov.yml
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Coverage is measured on every CI run for unit tests via go test -coverprofile=coverage.out ./..."
    - "coverage.out is uploaded to Codecov without a token (tokenless, public repo)"
    - "README.md Codecov badge URL references the master branch and displays current coverage"
    - "codecov.yml exists and suppresses noisy PR diff coverage comments"
  artifacts:
    - path: "codecov.yml"
      provides: "Codecov configuration"
      contains: "comment:"
    - path: ".github/workflows/ci.yml"
      provides: "Coverage upload in test job"
      contains: "codecov/codecov-action"
    - path: "README.md"
      provides: "Codecov badge"
      contains: "codecov.io/gh/webcane/docker-deploy/branch/master"
  key_links:
    - from: ".github/workflows/ci.yml (test job)"
      to: "codecov.io"
      via: "codecov/codecov-action@v4"
      pattern: "codecov/codecov-action@v4"
    - from: "README.md badge"
      to: "codecov.io/gh/webcane/docker-deploy"
      via: "badge URL"
      pattern: "branch/master/graph/badge"
---

<objective>
Wire Codecov coverage reporting into the CI pipeline and fix the README badge.

Purpose: Every CI run measures unit test coverage and uploads the result to Codecov so the badge in README reflects the real current coverage. The existing badge already exists in README but points to branch/main — the repo uses master, so the badge is broken and must be corrected.

Output: ci.yml extended with coverage flags and upload step; codecov.yml created; README badge URL corrected from /main/ to /master/.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/11-ci-tooling-polish/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add codecov.yml config file</name>
  <files>codecov.yml</files>
  <read_first>
    - /Users/mniedre/git/docker-deploy/.planning/phases/11-ci-tooling-polish/11-CONTEXT.md (D-07: minimal config to control PR diff coverage comments)
  </read_first>
  <action>
    Create codecov.yml at the repo root with minimal configuration per D-07. The file must suppress per-PR diff coverage comments (which are noisy for a small project) while keeping the overall coverage status check enabled.

    The file content must include:
    - `coverage.status.project.default.target: auto` — allow slight natural variation without breaking CI
    - `comment.layout: "reach, diff"` — compact PR comment if enabled
    - `comment.behavior: default`
    - `comment.require_changes: true` — only comment when coverage actually changes, suppressing noise on PRs that don't touch tested code

    Do NOT set `comment: false` — leave comments enabled but gated on actual changes.
  </action>
  <verify>
    <automated>test -f /Users/mniedre/git/docker-deploy/codecov.yml && grep -c "require_changes" /Users/mniedre/git/docker-deploy/codecov.yml | grep -q "1"</automated>
  </verify>
  <done>codecov.yml exists at repo root and contains a comment section with require_changes: true</done>
</task>

<task type="auto">
  <name>Task 2: Extend test job with coverage upload; fix README badge branch</name>
  <files>.github/workflows/ci.yml, README.md</files>
  <read_first>
    - /Users/mniedre/git/docker-deploy/.github/workflows/ci.yml (current test job structure — must extend Run tests step and add upload step)
    - /Users/mniedre/git/docker-deploy/README.md (badge line — must correct /main/ to /master/ in Codecov badge URL)
    - /Users/mniedre/git/docker-deploy/.planning/phases/11-ci-tooling-polish/11-CONTEXT.md (D-05 tokenless, D-06 unit tests only, D-08 codecov-action@v4)
  </read_first>
  <action>
    Two changes in this task:

    **1. ci.yml — extend the `test` job (per D-05, D-06, D-08):**

    Change the "Run tests" step from:
      `run: go test ./...`
    to:
      `run: go test -coverprofile=coverage.out ./...`

    Add a new step immediately after "Run tests":
    ```
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        files: coverage.out
        fail_ci_if_error: false
    ```

    No `token:` key — tokenless upload per D-05 (public repo). `fail_ci_if_error: false` means a Codecov outage does not break CI.

    Do NOT touch the `integration` job — coverage is unit tests only per D-06.
    Do NOT remove `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` from this job — that is handled by 11-02-PLAN.md after action versions are bumped.

    **2. README.md — fix Codecov badge URL (per must_haves):**

    The existing Codecov badge URL contains `/branch/main/graph/badge.svg`. The repo's default branch is `master`, so the badge is broken (Codecov cannot find branch `main`). Change:
      `https://codecov.io/gh/webcane/docker-deploy/branch/main/graph/badge.svg`
    to:
      `https://codecov.io/gh/webcane/docker-deploy/branch/master/graph/badge.svg`

    Also update the badge link href if it references `/branch/main`:
      `https://codecov.io/gh/webcane/docker-deploy` (the badge link href does not contain a branch path — leave it if it already resolves without branch).

    Touch only the badge line — no other README changes.
  </action>
  <verify>
    <automated>grep "codecov/codecov-action@v4" /Users/mniedre/git/docker-deploy/.github/workflows/ci.yml && grep "coverprofile=coverage.out" /Users/mniedre/git/docker-deploy/.github/workflows/ci.yml && grep "branch/master/graph/badge" /Users/mniedre/git/docker-deploy/README.md</automated>
  </verify>
  <done>ci.yml test job runs go test -coverprofile=coverage.out and uploads via codecov/codecov-action@v4 without a token; README.md Codecov badge URL references master branch</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → codecov.io | Coverage report uploaded from GitHub Actions to third-party service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Information Disclosure | coverage.out upload to Codecov | accept | Public repo — coverage data is not sensitive; tokenless upload is intentional (D-05) |
| T-11-01-02 | Denial of Service | fail_ci_if_error: false | mitigate | Codecov outages do not break CI; set fail_ci_if_error: false in action config |
</threat_model>

<verification>
All checks must pass on a CI run after these changes:
- `go test -coverprofile=coverage.out ./...` produces coverage.out in the test job
- codecov/codecov-action@v4 upload step runs without token
- README Codecov badge displays a percentage (not a "branch not found" error)
- grep "codecov/codecov-action@v4" .github/workflows/ci.yml returns a match
- grep "branch/master/graph/badge" README.md returns a match
- grep "branch/main" README.md returns no match (badge fully corrected)
</verification>

<success_criteria>
- codecov.yml exists at repo root with comment.require_changes: true
- ci.yml test job runs go test -coverprofile=coverage.out ./...
- ci.yml test job has a codecov/codecov-action@v4 upload step with no token and fail_ci_if_error: false
- README.md Codecov badge URL references /branch/master/ not /branch/main/
- No token or secret required for Codecov upload
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-tooling-polish/11-01-SUMMARY.md`
</output>

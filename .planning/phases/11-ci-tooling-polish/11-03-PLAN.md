---
phase: 11-ci-tooling-polish
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - .goreleaser.yaml
  - README.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "brew install webcane/docker-deploy/docker-deploy creates ~/.docker/cli-plugins/docker-deploy symlink automatically with no extra user steps"
    - "brew uninstall docker-deploy removes ~/.docker/cli-plugins/docker-deploy symlink with no dangling file"
    - "The caveats block instructing users to manually symlink is removed from the formula"
    - "GoReleaser still builds and pushes the Homebrew formula on release"
  artifacts:
    - path: ".goreleaser.yaml"
      provides: "Updated brews: block with post_install and custom_block"
      contains: "post_install"
  key_links:
    - from: ".goreleaser.yaml brews: post_install"
      to: "~/.docker/cli-plugins/docker-deploy"
      via: "ln -sf #{bin}/docker-deploy"
      pattern: "post_install"
    - from: ".goreleaser.yaml brews: custom_block"
      to: "~/.docker/cli-plugins/docker-deploy removal"
      via: "def uninstall"
      pattern: "def uninstall"
---

<objective>
Update the GoReleaser Homebrew formula config to automate the Docker CLI plugin symlink lifecycle.

Purpose: Currently the Homebrew formula installs the binary but leaves a caveats block instructing users to manually create the ~/.docker/cli-plugins/docker-deploy symlink. Per D-12 and D-13, post_install must create the symlink automatically and def uninstall must clean it up. Per D-14, the caveats block is removed entirely since it will no longer be accurate.

Output: .goreleaser.yaml brews: block updated with post_install, custom_block (def uninstall), and caveats: removed. README.md Homebrew section manual symlink instructions removed.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/11-ci-tooling-polish/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace caveats with post_install + def uninstall in .goreleaser.yaml brews: block; remove manual symlink from README.md</name>
  <files>.goreleaser.yaml, README.md</files>
  <read_first>
    - /Users/mniedre/git/docker-deploy/.goreleaser.yaml (full file — particularly the brews: block with current install:, test:, caveats: keys)
    - /Users/mniedre/git/docker-deploy/README.md (Homebrew install section — remove manual symlink instructions)
    - /Users/mniedre/git/docker-deploy/.planning/phases/11-ci-tooling-polish/11-CONTEXT.md (D-12, D-13, D-14 — exact Ruby block content specified)
  </read_first>
  <action>
    Modify the `brews:` block in .goreleaser.yaml. The current block has these keys: name, repository, commit_author, homepage, description, license, install:, test:, caveats:

    Make three changes:

    **1. Add post_install block (per D-12) — insert after the existing `install:` key:**

    Per D-12 exact specification, use:
        post_install: |
          mkdir -p "#{Dir.home}/.docker/cli-plugins"
          ln -sf "#{bin}/docker-deploy" "#{Dir.home}/.docker/cli-plugins/docker-deploy"

    The `#{bin}` interpolation is Homebrew Ruby DSL — it resolves to the Homebrew bin prefix where the binary was installed. `#{Dir.home}` resolves to the user's home directory in the Ruby context during brew install.

    **2. Add custom_block for def uninstall (per D-13) — insert after post_install:**

        custom_block: |
          def uninstall
            symlink = File.expand_path("~/.docker/cli-plugins/docker-deploy")
            File.delete(symlink) if File.exist?(symlink)
          end

    GoReleaser's `custom_block:` key injects raw Ruby into the generated formula class. The `def uninstall` method is called by Homebrew on `brew uninstall`. `File.expand_path("~...")` expands the tilde at uninstall time using the current user's home directory.

    **3. Remove caveats: block (per D-14):**
    Delete the entire `caveats: |` key and its multi-line string value from the brews: block. The block currently spans 7 lines:

        caveats: |
          Homebrew installs docker-deploy to the Homebrew prefix, but Docker CLI plugins
          must live in ~/.docker/cli-plugins/ to be discovered by the Docker CLI.

          Run the following to create the symlink:
            mkdir -p ~/.docker/cli-plugins
            ln -sf $(brew --prefix)/bin/docker-deploy ~/.docker/cli-plugins/docker-deploy

          After symlinking, verify with:
            docker deploy --help

    Remove all of this. Do not replace with any other caveats text.

    **Preserve unchanged:** name, repository, commit_author, homepage, description, license, install:, test:, and all top-level keys outside brews: (builds, archives, checksum, signs, release, snapshot).

    Final brews: key order should be: name → repository → commit_author → homepage → description → license → install → post_install → custom_block → test

    **Also update README.md Homebrew install section (same task, minimal change):**
    The README currently shows the manual symlink instructions in the Homebrew section. Once post_install automates this, those manual steps become misleading. In README.md, under "### Option 1: Homebrew (macOS / Linux)", remove the three lines starting with "Homebrew installs the binary to the Homebrew prefix. Add a symlink..." and the following code block containing `mkdir -p ~/.docker/cli-plugins` and `ln -sf $(brew --prefix)/bin/docker-deploy ~/.docker/cli-plugins/docker-deploy`. The section should end cleanly after `brew install docker-deploy`.
  </action>
  <verify>
    <automated>grep "post_install" /Users/mniedre/git/docker-deploy/.goreleaser.yaml && grep "def uninstall" /Users/mniedre/git/docker-deploy/.goreleaser.yaml && grep -c "caveats" /Users/mniedre/git/docker-deploy/.goreleaser.yaml | grep -q "^0$" && ! grep -q "ln -sf \$(brew --prefix)" /Users/mniedre/git/docker-deploy/README.md && ! grep -q "mkdir -p ~/.docker/cli-plugins" /Users/mniedre/git/docker-deploy/README.md</automated>
  </verify>
  <done>.goreleaser.yaml brews: block contains post_install and custom_block (def uninstall); the caveats: key is absent; README.md Homebrew section has no manual symlink instructions (no "ln -sf $(brew --prefix)" or "mkdir -p ~/.docker/cli-plugins" lines); all other brews: fields and top-level goreleaser keys are unchanged</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    .goreleaser.yaml updated: post_install creates symlink, def uninstall removes it, caveats removed. README.md Homebrew section cleaned of manual symlink instructions.
  </what-built>
  <how-to-verify>
    1. Run: grep -A5 "post_install" .goreleaser.yaml
       Expected: shows mkdir + ln -sf lines referencing #{Dir.home} and #{bin}

    2. Run: grep -A5 "custom_block" .goreleaser.yaml
       Expected: shows "def uninstall" with File.delete/File.exist? logic

    3. Run: grep "caveats" .goreleaser.yaml
       Expected: no output (caveats block fully removed)

    4. Run: goreleaser check
       If goreleaser is installed locally, this validates the config syntax. If not installed, skip this step.

    5. Open README.md and check the Homebrew section. It should show only:
       ```
       brew tap webcane/docker-deploy
       brew install docker-deploy
       ```
       with no manual symlink instructions following.

    6. Confirm the .goreleaser.yaml YAML is syntactically valid:
       python3 -c "import yaml,sys; yaml.safe_load(open('.goreleaser.yaml'))" && echo "YAML OK"
  </how-to-verify>
  <resume-signal>Type "approved" if the goreleaser config looks correct and YAML is valid, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brew install → ~/.docker/cli-plugins/ | post_install writes to user's home directory |
| brew uninstall → ~/.docker/cli-plugins/ | def uninstall removes from user's home directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | post_install writes symlink to ~/.docker/cli-plugins/ | accept | Homebrew install is an intentional privileged operation; user consents by running brew install |
| T-11-03-02 | Tampering | def uninstall removes symlink from ~/.docker/cli-plugins/ | accept | brew uninstall is an intentional operation; File.exist? guard prevents error on missing file; only removes the specific symlink, not the directory |
| T-11-03-03 | Elevation of Privilege | post_install runs as the user, not root | accept | Homebrew runs as the current user; ~/.docker/cli-plugins/ is user-owned — no privilege escalation |
</threat_model>

<verification>
After applying changes:
- grep "post_install" .goreleaser.yaml returns a match
- grep "def uninstall" .goreleaser.yaml returns a match
- grep "caveats" .goreleaser.yaml returns no match
- YAML is syntactically valid (python3 yaml.safe_load check passes)
- README Homebrew section has no manual symlink instructions (grep for "ln -sf $(brew --prefix)" returns no match; grep for "mkdir -p ~/.docker/cli-plugins" returns no match)
- Human checkpoint approved
</verification>

<success_criteria>
- .goreleaser.yaml brews: post_install creates ~/.docker/cli-plugins/docker-deploy symlink using #{bin} and #{Dir.home}
- .goreleaser.yaml brews: custom_block contains def uninstall that removes the symlink via File.delete with File.exist? guard
- caveats: key is absent from .goreleaser.yaml
- README.md Homebrew install section has no manual symlink instructions
- Human confirms YAML is syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-tooling-polish/11-03-SUMMARY.md`
</output>

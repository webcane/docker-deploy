---
phase: 11-ci-tooling-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - .github/dependabot.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 env var is absent from all workflow files"
    - "All actions in ci.yml and release.yml use current major version tags with no Node.js deprecation warnings"
    - ".github/dependabot.yml exists and configures weekly github-actions ecosystem updates"
  artifacts:
    - path: ".github/dependabot.yml"
      provides: "Automated weekly action version bumps"
      contains: "github-actions"
    - path: ".github/workflows/ci.yml"
      provides: "Updated action versions without FORCE workaround"
      contains: "actions/checkout@v4"
    - path: ".github/workflows/release.yml"
      provides: "Updated action versions without FORCE workaround"
      contains: "goreleaser/goreleaser-action@v6"
  key_links:
    - from: ".github/dependabot.yml"
      to: ".github/workflows/"
      via: "package-ecosystem: github-actions"
      pattern: "github-actions"
---

<objective>
Remove the FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 workaround from both workflow files and add Dependabot for automated future action version management.

Purpose: The FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 env var was a temporary workaround to suppress Node.js 20 deprecation warnings. Per D-09, it must be removed once real action bumps are in place. The current ci.yml and release.yml already use up-to-date action versions (checkout@v4, setup-go@v5, goreleaser-action@v6) — the only change needed is removing the workaround env var from each job. Dependabot (D-11) keeps versions current automatically going forward.

Output: FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 removed from ci.yml and release.yml; .github/dependabot.yml created with weekly github-actions schedule.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/11-ci-tooling-polish/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 from ci.yml and release.yml</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml</files>
  <read_first>
    - /Users/mniedre/git/docker-deploy/.github/workflows/ci.yml (two jobs: test and integration — both have the env var at job level)
    - /Users/mniedre/git/docker-deploy/.github/workflows/release.yml (one job: release — env var at job level)
    - /Users/mniedre/git/docker-deploy/.planning/phases/11-ci-tooling-polish/11-CONTEXT.md (D-09: remove after real bumps land; D-10: keep @vN pinning strategy)
  </read_first>
  <action>
    Per D-09, remove the `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"` env var from every job in both workflow files.

    In ci.yml:
    - Remove the `env:` block containing `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"` from the `test` job
    - Remove the `env:` block containing `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"` from the `integration` job
    - The `integration` job's inner env block (`TESTCONTAINERS_RYUK_DISABLED: "true"` on the step level) must be preserved — only remove the job-level env block

    In release.yml:
    - Remove the `env:` block containing `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"` from the `release` job
    - The job-level permissions block (`contents: write`, `id-token: write`) must be preserved
    - The step-level env block on goreleaser-action (`GITHUB_TOKEN`, `HOMEBREW_TAP_TOKEN`) must be preserved

    Do NOT change any action version tags in either file — action versions are correct as-is (@v4, @v5, @v6, @v3). Per D-10 major version tags are the pinning strategy, and current versions already satisfy this.

    After edits, verify no occurrence of FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 remains in either file.
  </action>
  <verify>
    <automated>grep -r "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24" /Users/mniedre/git/docker-deploy/.github/workflows/ | wc -l | tr -d ' ' | grep -q "^0$"</automated>
  </verify>
  <done>FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 does not appear in any file under .github/workflows/; all other workflow content is unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Create .github/dependabot.yml for weekly github-actions updates</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - /Users/mniedre/git/docker-deploy/.planning/phases/11-ci-tooling-polish/11-CONTEXT.md (D-11: weekly schedule, github-actions ecosystem, targeting .github/workflows/)
    - /Users/mniedre/git/docker-deploy/.github/workflows/ (directory that dependabot will monitor)
  </read_first>
  <action>
    Create .github/dependabot.yml with a single package-ecosystem entry for github-actions per D-11.

    The file must contain:
    - `version: 2`
    - One entry under `updates:`:
      - `package-ecosystem: "github-actions"`
      - `directory: "/"`  — GitHub Actions workflow files are always discovered from repo root
      - `schedule.interval: "weekly"`
      - `open-pull-requests-limit: 5` — cap concurrent PRs to avoid noise

    Do not add Go module or Docker ecosystem entries — this phase is actions-only.
  </action>
  <verify>
    <automated>test -f /Users/mniedre/git/docker-deploy/.github/dependabot.yml && grep -c "github-actions" /Users/mniedre/git/docker-deploy/.github/dependabot.yml | grep -q "1" && grep "weekly" /Users/mniedre/git/docker-deploy/.github/dependabot.yml</automated>
  </verify>
  <done>.github/dependabot.yml exists with package-ecosystem: github-actions, weekly schedule, and directory: "/"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependabot → workflow files | Automated PRs that bump action versions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | Dependabot auto-PRs bumping action versions | accept | PRs require human review before merge; major-version pinning (@vN) limits blast radius of auto-bumps |
| T-11-02-02 | Elevation of Privilege | Removing FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 | accept | The env var was a workaround — removing it reduces noise; actions already on current versions so no regression risk |
</threat_model>

<verification>
After applying changes:
- grep -r "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24" .github/workflows/ returns empty (zero matches)
- .github/dependabot.yml exists with version: 2
- dependabot.yml contains package-ecosystem: "github-actions"
- dependabot.yml contains schedule: interval: "weekly"
- CI job still has actions/checkout@v4 and actions/setup-go@v5
- Release job still has cosign-installer@v3 and goreleaser-action@v6
- TESTCONTAINERS_RYUK_DISABLED env var on the integration step is preserved
- goreleaser-action GITHUB_TOKEN and HOMEBREW_TAP_TOKEN env vars are preserved
</verification>

<success_criteria>
- FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 absent from all workflow files
- .github/dependabot.yml exists with weekly github-actions schedule
- All other workflow content (permissions, action versions, step-level env vars) unchanged
- No Node.js deprecation warnings in subsequent CI runs
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-tooling-polish/11-02-SUMMARY.md`
</output>

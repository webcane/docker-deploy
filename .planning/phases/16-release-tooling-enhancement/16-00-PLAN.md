---
phase: "16-release-tooling-enhancement"
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/gsd/release-tag.md
autonomous: true
requirements:
  - "SC-16-1"
  - "SC-16-2"
  - "SC-16-3"
  - "SC-16-4"
  - "SC-16-5"
  - "SC-16-6"

must_haves:
  truths:
    - "Pre-release checks (go test, lint, test-ci) run before the version bump question is asked"
    - "A go test failure hard-aborts release with no files changed"
    - "A lint failure triggers make lint-fix then re-runs lint; non-auto-fixable issues hard-abort"
    - "test-ci skips with a warning if no Docker socket is detected; aborts on failure if Docker is present"
    - "Each check prints its name before running (e.g. '▶ go test ./...')"
    - "STATE.md last_updated and last_activity are updated in the same release commit"
    - "Release commit body lists feat/fix/refactor/docs/test/ci/perf commits since previous tag; chore commits are excluded"
    - "If all commits since previous tag are chores, the commit uses subject line only (no body)"
  artifacts:
    - path: ".claude/commands/gsd/release-tag.md"
      provides: "Extended release skill with pre-release checks and STATE.md update"
      contains: "go test ./..."
  key_links:
    - from: ".claude/commands/gsd/release-tag.md Wave 0 checks"
      to: "Step 1 (detect latest tag)"
      via: "checks run BEFORE step 1 per D-01"
      pattern: "go test"
    - from: "release-tag.md Step 5"
      to: ".planning/STATE.md"
      via: "Edit last_updated and last_activity fields"
      pattern: "last_updated"
---

<objective>
Extend `.claude/commands/gsd/release-tag.md` with pre-release checks (Wave 0) and STATE.md update + commit body generation (Wave 1), combined into one plan because both changes modify the same skill file.

Purpose: A release should be one command — fail fast on broken tests or lint before any interactive prompts, and record the release in STATE.md automatically.

Output: Updated `release-tag.md` with Wave 0 pre-release check steps inserted before Step 1, STATE.md mutation added to Step 5, commit message body derived from git log added to Step 6, and guardrails updated.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/16-release-tooling-enhancement/16-CONTEXT.md
@/Users/mniedre/git/docker-deploy/Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Wave 0 pre-release check steps to release-tag.md</name>
  <files>.claude/commands/gsd/release-tag.md</files>
  <read_first>
    - .claude/commands/gsd/release-tag.md (full file — understand existing step structure before adding)
    - Makefile (test, test-ci, lint, lint-fix targets — exact commands to invoke)
    - .planning/phases/16-release-tooling-enhancement/16-CONTEXT.md (D-01 through D-05)
  </read_first>
  <action>
    Insert a new "Wave 0 — Pre-release checks" section BEFORE Step 1 in the `<process>` block. The new section contains three ordered sub-steps that MUST run before any user-facing prompts or file changes:

    **Wave 0 Step A — Unit tests (per D-02)**
    Print `▶ go test ./...` then run `make test`. If exit code is non-zero: print all output already shown (it streams), then print `ABORT: unit tests failed — fix failures before releasing` and exit with no file changes.

    **Wave 0 Step B — Linter with auto-fix retry gate (per D-03)**
    Print `▶ golangci-lint run ./...` then run `make lint`. If lint exits cleanly: print `PASS` and continue. If lint exits non-zero: immediately run `make lint-fix` (print `  Auto-fixing...`), then re-run `make lint`. If the second lint run still exits non-zero: print all remaining issues and print `ABORT: lint issues remain after auto-fix — resolve manually before releasing`, exit with no file changes. If the second lint run passes: print `PASS (auto-fixed)` and continue.

    **Wave 0 Step C — Integration tests with Docker auto-detect (per D-04)**
    Print `▶ test-ci (integration tests)`. Check for Docker socket using: `[ -S /var/run/docker.sock ] || [ -S $HOME/.colima/default/docker.sock ]`. If no socket found: print `  WARNING: Docker not detected — skipping integration tests` and continue (non-blocking). If socket found: run `make test-ci`. If `make test-ci` fails: print `ABORT: integration tests failed` and exit with no file changes. If it passes: print `PASS`.

    After all three sub-steps pass, print `All checks passed — proceeding with release` then continue to (the original) Step 1.

    Formatting rules:
    - Use `bash` fenced blocks for shell commands within the process (this is a skill file, not a PLAN.md action — fenced blocks ARE appropriate here)
    - Keep the existing step numbering (Step 1 through Step 9) unchanged; the new section is labelled "Wave 0 — Pre-release checks" as a header, not a numbered step
  </action>
  <verify>
    <automated>grep -c "Wave 0" /Users/mniedre/git/docker-deploy/.claude/commands/gsd/release-tag.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains a "Wave 0 — Pre-release checks" section appearing BEFORE "## Step 1"
    - File contains `make test` (unit test invocation)
    - File contains `make lint` followed by `make lint-fix` in an if-failure branch
    - File contains the Docker socket detection string `[ -S /var/run/docker.sock ] || [ -S $HOME/.colima/default/docker.sock ]`
    - File contains `WARNING: Docker not detected` as the skip message when no socket found
    - File contains `ABORT:` for each of the three hard-abort conditions
    - Existing Step 1 through Step 9 structure is intact (grep for "## Step 1 —" and "## Step 9 —" both return matches)
    - Each check starts with a `▶` prefix print
  </acceptance_criteria>
  <done>Wave 0 pre-release checks are the first section in the process block; all three checks (test, lint+fix-retry, test-ci) are documented with correct abort/skip/pass behaviour.</done>
</task>

<task type="auto">
  <name>Task 2: Add STATE.md update and commit body generation to release-tag.md</name>
  <files>.claude/commands/gsd/release-tag.md</files>
  <read_first>
    - .claude/commands/gsd/release-tag.md (current state after Task 1)
    - .planning/STATE.md (lines 1-15 — YAML frontmatter schema, specifically last_updated and last_activity fields)
    - .planning/phases/16-release-tooling-enhancement/16-CONTEXT.md (D-06 through D-13)
  </read_first>
  <action>
    Modify Step 5 and Step 6 of the existing process.

    **Step 5 extension (per D-10, D-11, D-12, D-13):**
    After updating README.md and INSTALL.md (existing Step 5 content), add a new sub-section "Update STATE.md". Instructions:
    - Read `.planning/STATE.md`
    - Set `last_updated:` to the current ISO 8601 timestamp (e.g. `"2026-05-27T14:30:00Z"`) — use `date -u +"%Y-%m-%dT%H:%M:%SZ"` to get the value
    - Set `last_activity:` to the string `{YYYY-MM-DD} -- Released {NEXT_TAG}` (e.g. `2026-05-27 -- Released v0.9.4`)
    - Do NOT change `milestone:` or any other frontmatter field (per D-12)
    - Use Edit to update only those two lines in the YAML frontmatter

    **Step 6 replacement (per D-06, D-07, D-08, D-09):**
    Replace the existing bare `git commit` command with a commit that includes a generated body:
    - Run `git log $PREV_TAG..HEAD --oneline` and capture output as `$LOG_LINES`
    - Filter out lines that start with `chore:` or `chore(` — all other prefixes (feat, fix, refactor, docs, test, ci, perf) are kept
    - Store filtered lines as `$BODY_LINES`
    - If `$BODY_LINES` is empty: commit with subject only — `git commit -m "chore: bump version to $NEXT_TAG"`
    - If `$BODY_LINES` is non-empty: commit with body:
      ```
      chore: bump version to $NEXT_TAG

      Changes since $CURRENT_TAG:
      - {line 1}
      - {line 2}
      ...
      ```
      Use a heredoc or multi-line `-m` invocation.

    **Step 6 git add update (per D-13):**
    Update `git add` in Step 6 to include `.planning/STATE.md` alongside README.md and INSTALL.md:
    `git add README.md INSTALL.md .planning/STATE.md`

    **Guardrails update:**
    Update the `<guardrails>` block at the end of the file to add:
    - `Only README.md, INSTALL.md, and .planning/STATE.md are modified; no other files change`
    (replacing the existing line that says "Only README.md and INSTALL.md are modified")
  </action>
  <verify>
    <automated>grep -c "last_updated" /Users/mniedre/git/docker-deploy/.claude/commands/gsd/release-tag.md</automated>
  </verify>
  <acceptance_criteria>
    - Step 5 contains a "Update STATE.md" sub-section
    - File contains `last_updated:` with instructions to set ISO 8601 timestamp via `date -u`
    - File contains `last_activity:` with the pattern `{YYYY-MM-DD} -- Released {NEXT_TAG}`
    - File contains a comment or instruction NOT to change `milestone:`
    - Step 6 `git add` line includes `.planning/STATE.md`
    - Step 6 contains `git log $PREV_TAG..HEAD --oneline` command
    - Step 6 contains filter logic excluding `chore:` and `chore(` prefixed lines
    - Step 6 contains the "Changes since $CURRENT_TAG:" body format with bullet lines
    - Step 6 has a conditional path for empty body (subject only)
    - Guardrails block mentions `STATE.md` in the "only these files" rule
    - Existing Step 7 (tag), Step 8 (push), Step 9 (report) are unchanged
  </acceptance_criteria>
  <done>STATE.md is mutated (last_updated, last_activity) and staged in the same release commit as README.md/INSTALL.md; commit body is derived from git log filtered to non-chore commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git log output → commit message | Untrusted commit subject lines injected into release commit body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-00-01 | Tampering | git log commit subjects in body | accept | Commit messages are developer-authored; no external input; low risk in a single-developer tool |
| T-16-00-02 | Denial of Service | make test-ci blocking release indefinitely | accept | Timeout is governed by Makefile test-ci target (15m); acceptable for a local developer tool |
</threat_model>

<verification>
After both tasks complete:

```bash
# Wave 0 section exists before Step 1
grep -n "Wave 0\|## Step 1" .claude/commands/gsd/release-tag.md
# Wave 0 line number must be LOWER than Step 1 line number

# All three check commands present
grep "make test\|make lint\|make test-ci" .claude/commands/gsd/release-tag.md

# Docker socket detection present
grep "docker.sock" .claude/commands/gsd/release-tag.md

# STATE.md fields present in Step 5
grep "last_updated\|last_activity" .claude/commands/gsd/release-tag.md

# git log filter present in Step 6
grep "git log.*--oneline\|chore:" .claude/commands/gsd/release-tag.md

# STATE.md staged in git add
grep "git add.*STATE.md" .claude/commands/gsd/release-tag.md
```
</verification>

<success_criteria>
- Wave 0 checks appear before any user-facing question or file change
- Unit tests → hard abort on failure
- Linter → auto-fix → re-lint → abort only on persistent failures
- Integration tests → skip with warning if no Docker; abort if Docker present and tests fail
- STATE.md last_updated and last_activity updated in same release commit
- Release commit body contains filtered git log entries (chore excluded); empty body if all chores
- All ROADMAP Phase 16 success criteria 1-6 verifiable from the updated skill file
</success_criteria>

<output>
After completion, create `.planning/phases/16-release-tooling-enhancement/16-00-SUMMARY.md`
</output>

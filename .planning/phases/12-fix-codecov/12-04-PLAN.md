---
phase: "12"
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - COMPARISON.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "COMPARISON.md has a '## Missing a tool?' section at the bottom of the file"
    - "The section contains a link to https://github.com/webcane/docker-deploy/issues"
    - "The comparison table itself is unchanged"
    - "The 'When to use' and 'When NOT to use' sections are unchanged"
  artifacts:
    - path: "COMPARISON.md"
      provides: "Feedback/contribution section appended at bottom"
      contains: "## Missing a tool?"
  key_links:
    - from: "COMPARISON.md ## Missing a tool?"
      to: "https://github.com/webcane/docker-deploy/issues"
      via: "markdown link"
      pattern: "github\\.com/webcane/docker-deploy/issues"
---

<objective>
Append a "## Missing a tool?" feedback section to the bottom of COMPARISON.md. The section links to GitHub Issues so users can suggest tools to add to the comparison table.

No changes to the comparison table or existing sections (per D-10).

Purpose: The table covers common alternatives but the field evolves. A visible feedback link invites users to suggest additions rather than assuming the table is exhaustive.
Output: COMPARISON.md with one new section appended after "## When NOT to use docker-deploy".
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/12-fix-codecov/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append "Missing a tool?" feedback section to COMPARISON.md</name>
  <files>COMPARISON.md</files>
  <read_first>
    - COMPARISON.md (read the full file — the current last section is "## When NOT to use docker-deploy"; the new section appends after it)
  </read_first>
  <action>
    Append the following content to the end of COMPARISON.md, after the last line of the "## When NOT to use docker-deploy" section (per D-08, D-09):

    A blank line separator, then:
    - `---` (horizontal rule)
    - Blank line
    - `## Missing a tool?`
    - Blank line
    - A 1–2 sentence welcoming paragraph that: (a) acknowledges the table may not cover every tool, and (b) invites users to open a GitHub Issue to suggest additions
    - A markdown link to `https://github.com/webcane/docker-deploy/issues` — use link text such as "Open an issue" or "GitHub Issues"

    Do not modify any existing content in COMPARISON.md — the comparison table, the "## When to use docker-deploy" section, and the "## When NOT to use docker-deploy" section must remain character-for-character identical to the current file.
  </action>
  <verify>
    <automated>grep -c "## Missing a tool?" COMPARISON.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Missing a tool?" COMPARISON.md` returns 1
    - `grep -c "github.com/webcane/docker-deploy/issues" COMPARISON.md` returns 1
    - `grep -c "## When NOT to use docker-deploy" COMPARISON.md` returns 1 (existing section preserved)
    - `grep -c "## When to use docker-deploy" COMPARISON.md` returns 1 (existing section preserved)
    - `grep -c "Comparison Table" COMPARISON.md` returns 1 (table heading preserved)
    - The "## Missing a tool?" heading appears AFTER "## When NOT to use docker-deploy" in the file (verify by line number: `grep -n "## " COMPARISON.md` shows "Missing a tool?" has a higher line number than "When NOT to use")
    - The section is at the bottom of the file — no content follows the issues link line (apart from a trailing newline)
  </acceptance_criteria>
  <done>"## Missing a tool?" section appended at bottom of COMPARISON.md with GitHub Issues link; all existing sections unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External link | Outbound link to github.com — public URL, no credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Spoofing | COMPARISON.md external link | accept | Link points to the project's own GitHub Issues page; no third-party domain involved |
</threat_model>

<verification>
Run all grep checks from acceptance_criteria.
Confirm `grep -n "## " COMPARISON.md` shows "Missing a tool?" with the highest line number among all `## ` headings.
</verification>

<success_criteria>
- COMPARISON.md ends with a "## Missing a tool?" section
- Section contains a markdown link to `https://github.com/webcane/docker-deploy/issues`
- Zero modifications to any existing content above the new section
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-codecov/12-04-SUMMARY.md`
</output>

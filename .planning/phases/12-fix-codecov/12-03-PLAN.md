---
phase: "12"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - INSTALL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "INSTALL.md exists at the project root"
    - "INSTALL.md has a section for each of the four install methods: Install script, Homebrew, Manual binary, go install"
    - "All four section headers use flat format without Option N: prefix"
    - "Homebrew section includes the brew tap and brew install commands"
    - "Manual binary section includes the tar extract, mkdir, mv, chmod steps and macOS Gatekeeper note"
    - "go install section includes the GOBIN instruction"
    - "Install script section includes both curl command blocks"
  artifacts:
    - path: "INSTALL.md"
      provides: "All four install methods with simplified headers"
      contains: "## Homebrew"
  key_links:
    - from: "README.md"
      to: "INSTALL.md"
      via: "relative markdown link [INSTALL.md](INSTALL.md)"
      pattern: "INSTALL\\.md"
---

<objective>
Create INSTALL.md at the project root containing all four install methods extracted from the current README.md Installation section. Use simplified flat headers (per D-06, D-07): "## Install script", "## Homebrew", "## Manual binary", "## go install" — no "Option N:" prefix.

This plan creates INSTALL.md only. README.md restructuring (removing the three non-primary methods) is done in plan 12-02 (also Wave 1 — independent).

Purpose: Concentrate all install documentation in one dedicated file so README stays focused on the primary path. Users who want alternative methods have a single reference point.
Output: INSTALL.md with all four install methods under simplified headers.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/12-fix-codecov/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create INSTALL.md with all four install methods</name>
  <files>INSTALL.md</files>
  <read_first>
    - README.md lines 11–65 (the complete Installation section — source of truth for all four method blocks, command strings, and notes that must be preserved verbatim in INSTALL.md)
  </read_first>
  <action>
    Create INSTALL.md at the project root. The file structure must be:

    1. `# Installation` — top-level heading
    2. A brief one-sentence intro: "Choose the method that best fits your environment."
    3. Four second-level sections in this order (per D-06, D-07 — flat headers, no "Option N:" prefix):
       - `## Install script` — copy the intro line and both curl command blocks from README "Option 2: Install script" verbatim
       - `## Homebrew` — copy the brew tap + brew install commands from README "Option 1: Homebrew" verbatim; add a brief note "(macOS / Linux)" as a paragraph under the header rather than in the header itself
       - `## Manual binary` — copy the numbered steps, tar/mv/chmod commands, macOS Gatekeeper note, and the xattr command from README "Option 3: Manual binary" verbatim
       - `## go install` — copy the GOBIN export and go install command, plus the GOBIN explanation paragraph, from README "Option 4: go install" verbatim

    Preserve all code blocks, command strings, and explanatory notes from the README source exactly. Do not invent new content. Do not omit any command or note from the source. The only changes are: header names (remove "Option N:"), and moving the platform qualifier ("macOS / Linux") from the Homebrew header into a paragraph.
  </action>
  <verify>
    <automated>grep -c "^## " INSTALL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " INSTALL.md` returns 4 (exactly four second-level sections)
    - `grep "^## " INSTALL.md` shows lines: "## Install script", "## Homebrew", "## Manual binary", "## go install" (in any order is acceptable, but all four must be present)
    - `grep -c "Option 1\|Option 2\|Option 3\|Option 4" INSTALL.md` returns 0 (no "Option N:" prefix anywhere)
    - `grep -c "brew tap webcane" INSTALL.md` returns 1
    - `grep -c "brew install docker-deploy" INSTALL.md` returns 1
    - `grep -c "curl -fsSL" INSTALL.md` returns 2 (both install script curl commands present)
    - `grep -c "xattr -d com.apple.quarantine" INSTALL.md` returns 1 (macOS Gatekeeper note preserved)
    - `grep -c "GOBIN" INSTALL.md` returns at least 1 (go install section present)
    - INSTALL.md file exists at the project root: `test -f INSTALL.md && echo exists`
  </acceptance_criteria>
  <done>INSTALL.md created with all four install methods under flat simplified headers; no Option N: prefixes; all commands and notes from README source preserved verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New file | INSTALL.md is a new documentation file; no code or config changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Information Disclosure | INSTALL.md | accept | Public documentation file; contains only already-public install instructions; no secrets or credentials |
</threat_model>

<verification>
Run all grep checks from acceptance_criteria.
Verify `test -f INSTALL.md` exits 0.
Confirm the four section headers match the required names exactly.
</verification>

<success_criteria>
- INSTALL.md exists at the project root
- Exactly four `## ` sections: Install script, Homebrew, Manual binary, go install
- Zero occurrences of "Option 1:", "Option 2:", "Option 3:", "Option 4:" in INSTALL.md
- All four install command sets are present and verbatim-correct
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-codecov/12-03-SUMMARY.md`
</output>

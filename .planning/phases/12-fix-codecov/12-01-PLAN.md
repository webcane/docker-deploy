---
phase: "12"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/docker-deploy/main.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "`docker deploy --help` shows 'Deploy a docker-compose project to a remote host' as the description"
    - "Running `docker-deploy docker-cli-plugin-metadata` returns JSON with ShortDescription 'Deploy a docker-compose project to a remote host'"
    - "The phrases 'remote VPS' do not appear in the Short or ShortDescription strings"
  artifacts:
    - path: "cmd/docker-deploy/main.go"
      provides: "Updated Short string and ShortDescription in main()"
      contains: "remote host"
  key_links:
    - from: "cmd.Short"
      to: "metadata.Metadata.ShortDescription"
      via: "both set to identical string"
      pattern: "Deploy a docker-compose project to a remote host"
---

<objective>
Update the plugin's one-liner description in two places in cmd/docker-deploy/main.go so that `docker deploy --help` and `docker-deploy docker-cli-plugin-metadata` both show the D-03 wording: "Deploy a docker-compose project to a remote host".

Purpose: "remote VPS" is too narrow — the plugin works with any SSH-accessible remote host, not just VPS instances. "remote host" is accurate and more inclusive.
Output: cmd/docker-deploy/main.go with updated Short and ShortDescription strings; no other changes.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/12-fix-codecov/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Short and ShortDescription to D-03 wording</name>
  <files>cmd/docker-deploy/main.go</files>
  <read_first>
    - cmd/docker-deploy/main.go (read the full file — lines 33–77 contain both strings to change)
  </read_first>
  <action>
    In cmd/docker-deploy/main.go make exactly two string replacements — no other changes to the file:

    1. Line ~40 inside main(): change the ShortDescription field in the metadata.Metadata struct literal from "Deploy a docker-compose project to a remote VPS" to "Deploy a docker-compose project to a remote host" (per D-01, D-03).

    2. Line ~58 inside buildDeployCmd(): change the Short field in the cobra.Command struct literal from "Deploy a docker-compose project to a remote VPS" to "Deploy a docker-compose project to a remote host" (per D-01, D-03).

    Both values MUST be identical after the change. Do not add a Long description (per D-02). Do not change any other field, import, comment, or line.
  </action>
  <verify>
    <automated>grep -c "remote VPS" cmd/docker-deploy/main.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "remote VPS" cmd/docker-deploy/main.go` returns 0
    - `grep -c "remote host" cmd/docker-deploy/main.go` returns 2 (exactly one occurrence in ShortDescription and one in Short)
    - `go build ./cmd/docker-deploy/` completes with exit code 0
    - The two updated string literals are character-for-character identical: "Deploy a docker-compose project to a remote host"
  </acceptance_criteria>
  <done>Both description strings updated to "Deploy a docker-compose project to a remote host"; build passes; no "remote VPS" remains in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source edit | No external input; purely a string literal change in Go source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | cmd/docker-deploy/main.go | accept | Documentation-only change; no logic altered; standard Go build CI catches compile errors |
</threat_model>

<verification>
`grep -c "remote VPS" cmd/docker-deploy/main.go` must return 0.
`grep "remote host" cmd/docker-deploy/main.go` must show two matching lines.
`go build ./cmd/docker-deploy/` must exit 0.
</verification>

<success_criteria>
- `docker deploy --help` (once built) displays "Deploy a docker-compose project to a remote host"
- `docker-deploy docker-cli-plugin-metadata` (once built) returns JSON where ShortDescription is "Deploy a docker-compose project to a remote host"
- Zero occurrences of "remote VPS" remain in cmd/docker-deploy/main.go
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-codecov/12-01-SUMMARY.md`
</output>

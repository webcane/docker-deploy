---
phase: 04-core-deploy-loop
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
requirements: [DEPLOY-01]

must_haves:
  truths:
    - "Config.ComposeFile is populated from --compose-file flag, compose_file in deploy.yaml, or auto-detected from the local project root"
    - "Auto-detection tries compose.yaml first, then docker-compose.yml"
    - "If no compose file is found by any method, Resolve() returns an error with message containing 'no compose file found'"
    - "Resolve() signature accepts composeFile string parameter alongside existing parameters"
  artifacts:
    - path: "internal/config/config.go"
      provides: "ComposeFile field on Config struct, compose_file field on TargetConfig, updated Resolve() signature"
      contains: "ComposeFile string"
    - path: "internal/config/config_test.go"
      provides: "TDD tests for all compose file resolution paths"
      exports: [TestResolveComposeFile]
  key_links:
    - from: "cmd/docker-deploy/main.go"
      to: "internal/config/config.go"
      via: "Resolve() call with composeFile flag value"
      pattern: "config\\.Resolve"
---

<objective>
Extend the Config struct and Resolve() function to support compose file resolution.

Purpose: The compose execution step (Plan 02) and the main.go wiring (Plan 03) both need the resolved compose filename. This plan adds the data model and resolution logic, enabling the deploy command to know which compose file to execute on the remote.

Output: Updated config package with ComposeFile field, TargetConfig.ComposeFile yaml key, Resolve() accepting --compose-file flag value, auto-detection from local project root, error on no-compose-file condition.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md

<interfaces>
<!-- Existing Config struct (internal/config/config.go) — extend, do not replace -->

type Config struct {
    Host     Host
    Path     string
    DryRun   bool
    Excludes []string
    Force    bool
    // ADD: ComposeFile string  (resolved compose filename, basename only, e.g. "compose.yaml")
}

type TargetConfig struct {
    Host    string   `yaml:"host"`
    Path    string   `yaml:"path"`
    Exclude []string `yaml:"exclude"`
    Force   bool     `yaml:"force"`
    // ADD: ComposeFile string `yaml:"compose_file"`
}

// Current Resolve signature:
func Resolve(flagHost, flagPath string, flagExcludes []string, flagForce bool, file FileConfig, projectName string) (Config, error)
// New Resolve signature:
func Resolve(flagHost, flagPath string, flagExcludes []string, flagForce bool, flagComposeFile string, file FileConfig, projectName string, localDir string) (Config, error)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: TDD — compose file resolution in Resolve()</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - internal/config/config.go — read full file to understand current struct layout and Resolve() body
    - internal/config/config_test.go — read if it exists, to understand existing test patterns
  </read_first>
  <behavior>
    - TestResolveComposeFile_FlagWins: when flagComposeFile="docker-compose.yml" is passed, resolved.ComposeFile == "docker-compose.yml", even if deploy.yaml has compose_file: "compose.yaml" and local dir has compose.yaml
    - TestResolveComposeFile_FileWins: when flagComposeFile="" and deploy.yaml target.compose_file="mycompose.yaml", resolved.ComposeFile == "mycompose.yaml"
    - TestResolveComposeFile_AutoDetectComposeYaml: when flagComposeFile="" and no deploy.yaml compose_file, and localDir contains compose.yaml, resolved.ComposeFile == "compose.yaml"
    - TestResolveComposeFile_AutoDetectDockerComposeYml: when flagComposeFile="" and no deploy.yaml compose_file, and localDir contains docker-compose.yml but not compose.yaml, resolved.ComposeFile == "docker-compose.yml"
    - TestResolveComposeFile_NoFileFound: when flagComposeFile="" and no deploy.yaml compose_file, and localDir contains neither compose.yaml nor docker-compose.yml, Resolve() returns error where errors.Is(err, nil)==false and err.Error() contains "no compose file found"
    - TestResolveComposeFile_PreservesExistingFields: Resolve() still correctly sets Host, Path, Excludes, Force after adding ComposeFile parameter
  </behavior>
  <action>
    RED phase: Create config_test.go (or add to existing) with the six test cases above. Tests must fail because ComposeFile field and updated Resolve() signature do not exist yet. Run: go test ./internal/config/... — expect compile errors. Commit with message: test(04-01): add failing tests for compose file resolution

    GREEN phase: Implement the following changes to internal/config/config.go:

    1. Add ComposeFile string field to Config struct (after Force).
    2. Add ComposeFile string field to TargetConfig struct with yaml tag "compose_file".
    3. Update Resolve() signature to accept two new parameters after flagForce: flagComposeFile string, localDir string. These come after the existing flagForce bool parameter and before file FileConfig.
    4. Inside Resolve(), after the Force resolution block, add compose file resolution:
       - If flagComposeFile != "": cfg.ComposeFile = flagComposeFile (flag wins, per D-07)
       - Else if file.Target.ComposeFile != "": cfg.ComposeFile = file.Target.ComposeFile (deploy.yaml wins, per D-07)
       - Else: auto-detect — check os.Stat(filepath.Join(localDir, "compose.yaml")), then os.Stat(filepath.Join(localDir, "docker-compose.yml")). Assign filename on first hit. If neither exists: return Config{}, fmt.Errorf("no compose file found; use --compose-file to specify one") (per D-08).
    5. Do NOT modify the Excludes or Force resolution logic.
    6. The resolved ComposeFile value is a basename only (no slashes) — per D-09. No path joining or cleaning needed.

    Run: go test ./internal/config/... — all six new tests plus any pre-existing tests must pass. Then run: go build ./... — expect compile errors in main.go because Resolve() signature changed. The main.go fix is Plan 03's responsibility; note the breakage in the commit message.

    Commit with message: feat(04-01): extend Config with ComposeFile; update Resolve() signature
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/docker-deploy && go test ./internal/config/... -v -run TestResolveComposeFile</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/config/... -v exits 0
    - TestResolveComposeFile_FlagWins passes
    - TestResolveComposeFile_FileWins passes
    - TestResolveComposeFile_AutoDetectComposeYaml passes
    - TestResolveComposeFile_AutoDetectDockerComposeYml passes
    - TestResolveComposeFile_NoFileFound passes and error string contains "no compose file found"
    - TestResolveComposeFile_PreservesExistingFields passes
    - grep "ComposeFile string" internal/config/config.go exits 0
    - grep 'yaml:"compose_file"' internal/config/config.go exits 0
    - grep "flagComposeFile string" internal/config/config.go exits 0
  </acceptance_criteria>
  <done>Resolve() accepts flagComposeFile and localDir; Config.ComposeFile is populated by flag > deploy.yaml > auto-detect priority; missing compose file returns error containing "no compose file found"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy.yaml → Config | User-controlled file value for compose_file is read into Config |
| --compose-file flag → Config | CLI-supplied filename enters the Config struct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | TargetConfig.ComposeFile (deploy.yaml) | mitigate | ComposeFile is validated to be a basename only (no slashes); Plan 02 validates filepath.Base(ComposeFile) == ComposeFile before constructing the remote command |
| T-04-01-02 | Spoofing | Auto-detect reads local filesystem via os.Stat | accept | Only checking existence of two known filenames; no directory traversal; low risk |
| T-04-01-03 | Information Disclosure | Resolve() error message in "no compose file found" case | accept | Error message reveals local directory was checked; no sensitive data exposed |
</threat_model>

<verification>
go test ./internal/config/... -v exits 0 with all TestResolveComposeFile_* cases passing
grep "ComposeFile string" internal/config/config.go exits 0
</verification>

<success_criteria>
Config.ComposeFile is populated correctly from all three resolution sources (flag, deploy.yaml, auto-detect). Missing compose file returns a clear error. All six TDD tests pass. Resolve() signature matches what Plan 03 will call.
</success_criteria>

<output>
After completion, create .planning/phases/04-core-deploy-loop/04-01-SUMMARY.md
</output>

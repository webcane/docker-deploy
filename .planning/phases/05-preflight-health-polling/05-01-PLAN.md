---
phase: 05-preflight-health-polling
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
requirements: [HEALTH-01, HEALTH-02, HEALTH-03]

must_haves:
  truths:
    - "Config struct has HealthTimeout int and HealthInterval int fields"
    - "TargetConfig and FileConfig have health_timeout and health_interval yaml keys"
    - "Resolve() applies flag > file > default precedence: HealthTimeout default 60, HealthInterval default 5"
    - "All existing Resolve() tests continue to pass"
  artifacts:
    - path: "internal/config/config.go"
      provides: "Config.HealthTimeout and Config.HealthInterval fields; TargetConfig yaml keys; Resolve() defaults"
      contains: "HealthTimeout int"
    - path: "internal/config/config_test.go"
      provides: "TDD tests for HealthTimeout and HealthInterval resolution"
      exports: []
  key_links:
    - from: "internal/config/config.go"
      to: "cmd/docker-deploy/main.go"
      via: "Config struct"
      pattern: "HealthTimeout"
    - from: "internal/config/config.go"
      to: "internal/health/poll.go"
      via: "Config.HealthTimeout and Config.HealthInterval fields"
      pattern: "HealthInterval"
---

<objective>
Extend the Config struct with HealthTimeout and HealthInterval fields so health polling in Plan 03 can read configurable parameters from the resolved config.

Purpose: The health polling package (Plan 03) and main.go (Plan 04) both consume Config. Adding these fields here in Wave 1 unblocks both downstream plans from running in parallel in Wave 2.
Output: Updated internal/config/config.go with two new int fields and updated Resolve() defaults; updated config_test.go with TDD coverage.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/05-preflight-health-polling/05-CONTEXT.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend Config with HealthTimeout and HealthInterval (TDD)</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>

  <read_first>
    - internal/config/config.go — current Config struct, TargetConfig struct, FileConfig struct, Resolve() signature and body; existing Force and ComposeFile precedence patterns to replicate
    - internal/config/config_test.go — existing test cases for Resolve() to understand test structure and avoid breaking them
  </read_first>

  <behavior>
    - Test: Resolve() with no file config and no flags → Config.HealthTimeout == 60, Config.HealthInterval == 5
    - Test: Resolve() with file config health_timeout=30, health_interval=10 → Config.HealthTimeout == 30, Config.HealthInterval == 10
    - Test: Resolve() with file config health_timeout=30 but flag override flagHealthTimeout=45 → Config.HealthTimeout == 45 (flag wins)
    - Test: Resolve() with zero file values → falls back to defaults 60 and 5 (zero means unset in YAML)
    - Test: All existing Resolve() test cases continue to pass without modification
  </behavior>

  <action>
    RED phase: In config_test.go, add test cases for HealthTimeout and HealthInterval using the same table-driven style as existing tests. Tests must fail before any implementation change.

    GREEN phase in config.go:
    1. Add to TargetConfig struct: `HealthTimeout int` with yaml tag `yaml:"health_timeout"` and `HealthInterval int` with yaml tag `yaml:"health_interval"`.
    2. Add to Config struct: `HealthTimeout int` and `HealthInterval int` (no yaml tags — Config is runtime only).
    3. Update Resolve() signature to accept two new int parameters: `flagHealthTimeout int` and `flagHealthInterval int` (append after existing flagComposeFile parameter, before file FileConfig). These are 0 when not set by caller — treat 0 as "not set" (same as empty string pattern used for flagHost and flagPath). NOTE: In Phase 5, these flag parameters are not registered as CLI flags (D-03 defers --verbose; health flags via deploy.yaml only). Pass 0 from all callers. The signature accepts them for future-proofing without registering cobra flags.
    4. In Resolve() body, add resolution block after ComposeFile resolution:
       - HealthTimeout: flagHealthTimeout > 0 → use it; else file.Target.HealthTimeout > 0 → use it; else default 60
       - HealthInterval: flagHealthInterval > 0 → use it; else file.Target.HealthInterval > 0 → use it; else default 5
    5. Update all calls to Resolve() in cmd/docker-deploy/main.go to pass 0, 0 as the two new trailing arguments before fileConfig.

    REFACTOR: Verify no duplication; confirm existing tests pass; confirm new tests pass.
  </action>

  <verify>
    <automated>cd /Users/mniedre/git/docker-deploy && go test ./internal/config/... -v -count=1</automated>
  </verify>

  <acceptance_criteria>
    - config.go Config struct contains fields `HealthTimeout int` and `HealthInterval int`
    - config.go TargetConfig struct contains `HealthTimeout int \`yaml:"health_timeout"\`` and `HealthInterval int \`yaml:"health_interval"\``
    - Resolve() signature has parameters flagHealthTimeout int and flagHealthInterval int
    - go test ./internal/config/... exits 0 with all tests passing
    - go build ./... exits 0 (no compilation errors from updated Resolve() call sites in main.go)
    - Test for default values: Resolve() with zero flag values and empty FileConfig returns HealthTimeout == 60 and HealthInterval == 5
  </acceptance_criteria>

  <done>Config.HealthTimeout and Config.HealthInterval fields exist with correct defaults (60s/5s), file-config override works, zero-value flag falls through to file then default, all config tests pass, main.go compiles with updated Resolve() call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy.yaml → Config | Operator-controlled file; parsed YAML integers could be zero/negative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Config.HealthTimeout / HealthInterval | mitigate | Resolve() treats 0 as unset and applies defaults; negative values behave as 0 (Go int comparison > 0 check gates both flag and file values) |
</threat_model>

<verification>
go build ./... passes
go test ./internal/config/... passes
Config struct has HealthTimeout and HealthInterval int fields
Resolve() returns HealthTimeout=60 and HealthInterval=5 when no overrides are set
</verification>

<success_criteria>
Config extension is complete and all existing tests pass. Plans 02 and 03 can proceed in parallel using Config.HealthTimeout and Config.HealthInterval without any further changes to the config package.
</success_criteria>

<output>
After completion, create `.planning/phases/05-preflight-health-polling/05-01-SUMMARY.md`
</output>

---
phase: 13-cli-subcommands-deploy-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/docker-deploy/main.go
  - Makefile
  - .goreleaser.yaml
autonomous: true
requirements:
  - 13-02
must_haves:
  truths:
    - "`docker deploy version` is a registered cobra subcommand that prints version info and exits 0"
    - "Tagged release builds show semver tag as version; dev builds show 'dev'"
    - "Git commit short hash is printed in all builds; build timestamp is printed in tagged builds only"
    - "OS/Arch is derived at runtime from runtime.GOOS + '/' + runtime.GOARCH"
    - "Makefile build target injects gitCommit and buildTime ldflags alongside version"
    - "GoReleaser ldflags block injects gitCommit and buildTime alongside version"
  artifacts:
    - path: "cmd/docker-deploy/main.go"
      provides: "var gitCommit = 'unknown'; var buildTime = 'unknown'; buildVersionCmd(); runVersion()"
      exports: ["buildVersionCmd"]
    - path: "Makefile"
      provides: "extended build target with -X main.gitCommit and -X main.buildTime"
      contains: "git rev-parse --short HEAD"
    - path: ".goreleaser.yaml"
      provides: "extended ldflags with -X main.gitCommit={{.ShortCommit}} and -X main.buildTime={{.Date}}"
      contains: "ShortCommit"
  key_links:
    - from: "buildDeployCmd() in main.go"
      to: "buildVersionCmd()"
      via: "cmd.AddCommand(buildVersionCmd())"
      pattern: "AddCommand.*buildVersionCmd"
    - from: ".goreleaser.yaml ldflags"
      to: "var gitCommit, var buildTime in main.go"
      via: "-X main.gitCommit={{.ShortCommit}} -X main.buildTime={{.Date}}"
      pattern: "ShortCommit"
---

<objective>
Add the `docker deploy version` subcommand and inject three ldflags variables (version, gitCommit, buildTime) at build time via Makefile and GoReleaser.

Purpose: Users need a standard way to inspect which binary version they have installed; CI/release pipelines need build metadata in the binary itself.
Output: New `buildVersionCmd()` + `runVersion()` in main.go; two new package-level vars; extended Makefile build target; extended GoReleaser ldflags block. No new files — all changes in existing files.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md

<interfaces>
<!-- Key patterns the executor needs — extracted from codebase. -->

From cmd/docker-deploy/main.go — existing ldflags var (line 26):
```go
var version = "dev"
```
Add alongside it:
```go
var gitCommit = "unknown"
var buildTime = "unknown"
```

From cmd/docker-deploy/main.go — subcommand registration pattern in buildDeployCmd() (lines 46-77):
```go
cmd := &cobra.Command{Use: "deploy", Short: "..."}
// ... flags ...
cmd.AddCommand(buildVersionCmd())  // ADD THIS LINE
return cmd
```

Required version output format (D-01, D-03):
- Tagged build (buildTime != "unknown"):
  ```
  Docker Deploy Version v0.6.3
    Git commit:        de40ad0
   Built:             2026-04-20T14:57:44Z
   OS/Arch:           darwin/arm64
  ```
- Dev build (buildTime == "unknown"):
  ```
  Docker Deploy Version dev
    Git commit:        abc1234
   OS/Arch:           darwin/arm64
  ```

From Makefile — current build target (line 3-5):
```makefile
build:
	mkdir -p bin
	go build -ldflags "-X main.version=dev" -o bin/docker-deploy ./cmd/docker-deploy/
```
Must extend to:
```makefile
build:
	mkdir -p bin
	go build -ldflags "-X main.version=dev \
		-X main.gitCommit=$(shell git rev-parse --short HEAD 2>/dev/null || echo unknown) \
		-X main.buildTime=$(shell date -u +%FT%TZ 2>/dev/null || echo unknown)" \
		-o bin/docker-deploy ./cmd/docker-deploy/
```

From .goreleaser.yaml — current ldflags block (lines 6-9):
```yaml
ldflags:
  - -s
  - -w
  - -X main.version={{.Version}}
```
Must extend to:
```yaml
ldflags:
  - -s
  - -w
  - -X main.version={{.Version}}
  - -X main.gitCommit={{.ShortCommit}}
  - -X main.buildTime={{.Date}}
```

From cmd/docker-deploy/main_test.go — test package pattern:
```go
package main  // white-box; calls unexported functions like buildDeployCmd()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add package-level vars, buildVersionCmd, runVersion to main.go; register subcommand</name>
  <files>cmd/docker-deploy/main.go, cmd/docker-deploy/main_test.go</files>
  <read_first>
    - cmd/docker-deploy/main.go — full file: locate var version line, buildDeployCmd function signature and body, existing subcommand patterns, import block
    - cmd/docker-deploy/main_test.go — existing tests to understand test style and avoid conflicts
  </read_first>
  <behavior>
    - Test TestVersionCmd_Registered: buildDeployCmd() has a subcommand with Use=="version"
    - Test TestVersionCmd_DevOutput: when buildTime=="unknown", runVersion() prints "Docker Deploy Version dev" and does NOT include "Built:" line
    - Test TestVersionCmd_TaggedOutput: when buildTime is a non-"unknown" value, runVersion() output includes "Built:" line
    - Test TestVersionCmd_ExitZero: buildVersionCmd().RunE returns nil (exit 0)
  </behavior>
  <action>
    In cmd/docker-deploy/main.go:
    1. After `var version = "dev"` (line 26), add two new package-level vars: `var gitCommit = "unknown"` and `var buildTime = "unknown"`. These are the ldflags injection targets per D-05.
    2. Add `"runtime"` to the import block (for runtime.GOOS and runtime.GOARCH).
    3. Add `buildVersionCmd() *cobra.Command` — returns a cobra.Command with Use="version", Short="Print version information", SilenceUsage=true. RunE calls runVersion(). No flags needed.
    4. Add `runVersion() error` — writes to os.Stdout (not os.Stderr — version info is a success output per shared pattern):
       - If buildTime != "unknown": print 4-line format with Built line (D-01)
       - If buildTime == "unknown": print 3-line dev format omitting Built line (D-03)
       - OS/Arch via runtime.GOOS + "/" + runtime.GOARCH
    5. Inside buildDeployCmd(), add cmd.AddCommand(buildVersionCmd()) before the return statement. Do NOT add a --version flag (D-04).

    In cmd/docker-deploy/main_test.go:
    Add TestVersionCmd_Registered, TestVersionCmd_DevOutput, TestVersionCmd_TaggedOutput, TestVersionCmd_ExitZero tests. Set package-level vars (version, gitCommit, buildTime) directly in tests to avoid ldflags dependency in unit tests.
  </action>
  <verify>
    <automated>go test ./cmd/docker-deploy/... -run TestVersionCmd -v</automated>
  </verify>
  <done>All TestVersionCmd* tests pass; go build ./... exits 0; grep "buildVersionCmd" cmd/docker-deploy/main.go finds a function definition and an AddCommand call</done>
</task>

<task type="auto">
  <name>Task 2: Extend Makefile build target and GoReleaser ldflags with gitCommit and buildTime</name>
  <files>Makefile, .goreleaser.yaml</files>
  <read_first>
    - Makefile — full file: current build target with single -X main.version=dev ldflag
    - .goreleaser.yaml — full file: current ldflags block under builds[0].ldflags
  </read_first>
  <action>
    In Makefile, update the `build` target. Replace the current single-ldflag go build line with a multi-ldflag version that also injects gitCommit and buildTime:
    - -X main.gitCommit injected via $(shell git rev-parse --short HEAD 2>/dev/null || echo unknown)
    - -X main.buildTime injected via $(shell date -u +%FT%TZ 2>/dev/null || echo unknown)
    - Keep -X main.version=dev unchanged
    - Keep -o bin/docker-deploy ./cmd/docker-deploy/ unchanged
    Use backslash continuation for readability (tab-indented per Makefile convention).

    In .goreleaser.yaml, under builds[0].ldflags, append two new entries after the existing -X main.version={{.Version}} line:
    - -X main.gitCommit={{.ShortCommit}}
    - -X main.buildTime={{.Date}}
    Keep -s and -w unchanged. Do not modify any other section of .goreleaser.yaml.

    Note: GoReleaser {{.Date}} produces RFC3339 format (e.g. 2026-04-20T14:57:44Z). This is acceptable — D-01 shows a style example, not a locked format constraint.
  </action>
  <verify>
    <automated>make build && ./bin/docker-deploy version</automated>
  </verify>
  <done>make build succeeds; ./bin/docker-deploy version prints "Docker Deploy Version dev" with Git commit and OS/Arch lines; Makefile contains "git rev-parse --short HEAD"; .goreleaser.yaml contains "ShortCommit" and "buildTime"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ldflags injection → package vars | Build system injects version string; no user input at runtime |
| version subcommand stdout | Output is assembled from build-time constants and stdlib runtime — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Information Disclosure | version subcommand output | accept | Git commit hash and build timestamp are intentionally public metadata; no credentials or secrets injected |
| T-13-02-02 | Tampering | ldflags in Makefile | accept | Build system is developer-controlled; ldflags only inject version metadata, not security-relevant values |
</threat_model>

<verification>
- make build exits 0
- ./bin/docker-deploy version prints a line starting with "Docker Deploy Version"
- go test ./cmd/docker-deploy/... exits 0
- go test ./... exits 0 (full suite)
- grep -c "gitCommit" .goreleaser.yaml returns 1
- grep -c "ShortCommit" .goreleaser.yaml returns 1
</verification>

<success_criteria>
`docker deploy version` subcommand is registered and prints Docker CLI-style version output (version, git commit, OS/arch, and built timestamp when available). Makefile and GoReleaser both inject all three ldflags vars. Full test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-cli-subcommands-deploy-ux/13-02-SUMMARY.md`
</output>

---
phase: 13-cli-subcommands-deploy-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
requirements:
  - 13-01
must_haves:
  truths:
    - "deploy.yaml is loaded from the directory where the command is run (os.Getwd()), not from a hardcoded path"
    - "config.LoadFile() accepts a cwd argument and constructs the config file path as filepath.Join(cwd, 'deploy.yaml')"
    - "Unit tests confirm LoadFile resolves the file relative to the provided cwd, not from os.Getwd() inside the function"
  artifacts:
    - path: "internal/config/config.go"
      provides: "LoadFile(cwd string) using filepath.Join(cwd, 'deploy.yaml'); no os.Getwd() call inside LoadFile"
    - path: "internal/config/config_test.go"
      provides: "TestLoadFile tests confirming cwd-relative path construction"
  key_links:
    - from: "cmd/docker-deploy/main.go"
      to: "internal/config/config.go"
      via: "config.LoadFile(cwd) — cwd obtained from os.Getwd() in runDeploy/runDryRun"
      pattern: "config\\.LoadFile\\(cwd\\)"
---

<objective>
Ensure deploy.yaml is always resolved relative to the caller-provided cwd argument in config.LoadFile().

Purpose: If config.LoadFile() internally calls os.Getwd() or uses a hardcoded lookup, it will fail when called from subcommands (validate) or when the caller changes directory. Callers already pass cwd — LoadFile must use it faithfully.
Output: config.LoadFile(cwd string) that builds the config path as filepath.Join(cwd, "deploy.yaml") with no internal os.Getwd() call; updated unit tests confirming the behavior.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md

<interfaces>
<!-- Key signatures and patterns the executor needs — extracted from codebase. -->

From internal/config/config.go (current LoadFile signature):
```go
func LoadFile(cwd string) (FileConfig, error)
```

From cmd/docker-deploy/main.go (call sites — cwd already passed by caller):
```go
// runDryRun (line 120):
fileConfig, err := config.LoadFile(cwd)

// runDeploy (line 196):
fileConfig, err := config.LoadFile(cwd)
```

Current cwd acquisition pattern in callers (lines 113-117 in main.go):
```go
cwd, err := os.Getwd()
if err != nil {
    return fmt.Errorf("getting working directory: %w", err)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit and fix LoadFile cwd handling + add targeted test</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - internal/config/config.go — full file: find LoadFile implementation, check if it calls os.Getwd() internally or constructs the path from cwd correctly
    - internal/config/config_test.go — existing TestLoadFile tests to understand current coverage and avoid duplication
  </read_first>
  <behavior>
    - Test: LoadFile called with an explicit tmpDir returns the config from deploy.yaml in that dir (no os.Getwd() needed)
    - Test: LoadFile called with a dir containing no deploy.yaml returns empty FileConfig and nil error (not-found is not an error)
    - Test: LoadFile called with a dir containing invalid YAML returns a non-nil error
  </behavior>
  <action>
    Read internal/config/config.go. Locate the LoadFile function. Verify the config file path is constructed as filepath.Join(cwd, "deploy.yaml") and that no internal os.Getwd() call exists inside LoadFile.

    If LoadFile calls os.Getwd() internally: remove that call and replace with filepath.Join(cwd, "deploy.yaml") using the cwd parameter already provided.

    If LoadFile already uses the cwd parameter correctly: no change to config.go — only tests need confirming.

    In config_test.go, add or verify a test named TestLoadFile_CwdRelative that:
    - Creates a temporary directory via t.TempDir()
    - Writes a minimal valid deploy.yaml to that directory
    - Calls config.LoadFile(tmpDir) and asserts the returned FileConfig is non-zero
    - Calls config.LoadFile on a second empty tmpDir and asserts nil error + zero FileConfig

    All existing tests in config_test.go must continue to pass.
  </action>
  <verify>
    <automated>go test ./internal/config/... -run TestLoadFile -v</automated>
  </verify>
  <done>go test ./internal/config/... exits 0; config.LoadFile has no internal os.Getwd() call (grep -n "os\.Getwd" internal/config/config.go returns no results or only results outside LoadFile); TestLoadFile_CwdRelative passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cwd argument → file path | Caller-provided cwd is joined with a fixed filename ("deploy.yaml") — no user-controlled filename component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | filepath.Join(cwd, "deploy.yaml") | accept | cwd comes from os.Getwd() in the caller — a safe stdlib call; filename is a hardcoded constant, not user input |
| T-13-01-02 | Information Disclosure | deploy.yaml parse error messages | accept | Error messages describe parse failures but never echo file content; low-value target |
</threat_model>

<verification>
- grep -n "os\.Getwd" internal/config/config.go — must return 0 lines inside the LoadFile function body
- go test ./internal/config/... exits 0
- go test ./... exits 0 (full suite)
</verification>

<success_criteria>
config.LoadFile(cwd) constructs the path as filepath.Join(cwd, "deploy.yaml") using only the provided cwd argument. Unit tests confirm the function reads from the provided directory. Full test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-cli-subcommands-deploy-ux/13-01-SUMMARY.md`
</output>

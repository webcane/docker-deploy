---
phase: 03-file-copy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
requirements: [FILES-03]
must_haves:
  truths:
    - "Config.Excludes is populated from flag > deploy.yaml > built-in defaults in the correct precedence order"
    - "Config.Force is populated from flag > deploy.yaml > defaults"
    - "Built-in default excludes are always present in Config.Excludes regardless of user input"
    - "User-supplied excludes extend, never replace, the built-in list"
    - "Existing Resolve() tests pass without modification"
  artifacts:
    - path: internal/config/config.go
      provides: "Extended Config, TargetConfig structs and updated Resolve() signature"
      contains: "Excludes []string"
    - path: internal/config/config_test.go
      provides: "Table-driven tests for Excludes and Force precedence"
      contains: "TestResolveExcludes"
  key_links:
    - from: internal/config/config.go
      to: internal/config/config_test.go
      via: "same package — test directly exercises Resolve()"
      pattern: "Resolve(.*Excludes"
---

<objective>
Extend the config package to support the Phase 3 file-filter model.

Purpose: `internal/config` is the single source of truth for resolved runtime config. Plans 02 and 03 both need `Config.Excludes []string` and `Config.Force bool`. This plan adds those fields before the SFTP package is written so downstream plans have a stable contract.

Output: Extended `Config` and `TargetConfig` structs, updated `Resolve()` with Excludes/Force precedence, table-driven tests that cover all precedence combinations.
</objective>

<execution_context>
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mniedre/git/docker-deploy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mniedre/git/docker-deploy/.planning/PROJECT.md
@/Users/mniedre/git/docker-deploy/.planning/ROADMAP.md
@/Users/mniedre/git/docker-deploy/.planning/STATE.md
@/Users/mniedre/git/docker-deploy/.planning/phases/03-file-copy/03-CONTEXT.md
</context>

<interfaces>
<!-- Current contracts the executor MUST preserve and extend. -->

From internal/config/config.go (current state):
```go
type TargetConfig struct {
    Host string `yaml:"host"`
    Path string `yaml:"path"`
}

type FileConfig struct {
    Version int          `yaml:"version"`
    Target  TargetConfig `yaml:"target"`
}

type Config struct {
    Host    Host
    Path    string
    DryRun  bool
}

func Resolve(flagHost, flagPath string, file FileConfig, projectName string) (Config, error)
```

Required extension (what this plan MUST produce):
```go
type TargetConfig struct {
    Host    string   `yaml:"host"`
    Path    string   `yaml:"path"`
    Exclude []string `yaml:"exclude"`
    Force   bool     `yaml:"force"`
}

type Config struct {
    Host     Host
    Path     string
    DryRun   bool
    Excludes []string   // merged: builtInExcludes + file.Target.Exclude + flagExcludes, deduplicated
    Force    bool       // flag > deploy.yaml > false
}

// Updated signature — flagExcludes is the repeatable --exclude flag values slice
func Resolve(flagHost, flagPath string, flagExcludes []string, flagForce bool, file FileConfig, projectName string) (Config, error)
```

Built-in default excludes constant (must live in config.go):
```go
var defaultExcludes = []string{
    ".git/", "node_modules/", "vendor/", "*.log", ".DS_Store", "__pycache__/",
}
```

Merge logic: defaultExcludes + file.Target.Exclude + flagExcludes (in that order, deduplicated by string equality — preserving insertion order, later duplicates dropped).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend config structs and Resolve() signature</name>
  <files>internal/config/config.go</files>
  <read_first>
    - internal/config/config.go — read the full file; understand existing TargetConfig, FileConfig, Config structs and the current Resolve() function body before editing
    - .planning/phases/03-file-copy/03-CONTEXT.md — D-01 through D-06 for exclude model, D-11/D-12 for force and yaml schema placement
  </read_first>
  <behavior>
    - Resolve(flagHost="", flagPath="", flagExcludes=nil, flagForce=false, FileConfig{}, "p") returns Config.Excludes == defaultExcludes and Config.Force == false
    - Resolve with file.Target.Exclude=["*.tmp"] returns Config.Excludes == append(defaultExcludes, "*.tmp")
    - Resolve with flagExcludes=["logs/"] and file.Target.Exclude=["*.tmp"] returns Config.Excludes == append(append(defaultExcludes, "*.tmp"), "logs/")
    - Resolve with duplicate entries (e.g., ".git/" in flagExcludes) deduplicates — ".git/" appears only once
    - Resolve with file.Target.Force=true and flagForce=false returns Config.Force == true
    - Resolve with file.Target.Force=false and flagForce=true returns Config.Force == true (flag wins)
    - Existing Resolve() callers in cmd/docker-deploy/main.go will need to be updated (they pass 4 args currently; new signature takes 6) — note this in code comments
  </behavior>
  <action>
    Add `Exclude []string` with yaml tag `yaml:"exclude"` and `Force bool` with yaml tag `yaml:"force"` to TargetConfig. Add `Excludes []string` and `Force bool` to Config. Add package-level `var defaultExcludes = []string{".git/", "node_modules/", "vendor/", "*.log", ".DS_Store", "__pycache__/"}`. Update Resolve() signature to `Resolve(flagHost, flagPath string, flagExcludes []string, flagForce bool, file FileConfig, projectName string) (Config, error)`. Inside Resolve(), build cfg.Excludes by: starting with a copy of defaultExcludes, appending file.Target.Exclude entries not already present, then appending flagExcludes entries not already present (deduplication by string equality, preserving insertion order). Build cfg.Force as: flagForce || file.Target.Force. Do not change Host or Path resolution logic. Add a comment above Resolve() noting the new parameters and that cmd/docker-deploy/main.go must be updated to pass the two new args.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/docker-deploy && go build ./internal/config/ && go vet ./internal/config/</automated>
  </verify>
  <done>config.go compiles, TargetConfig has Exclude and Force yaml-tagged fields, Config has Excludes and Force fields, defaultExcludes var contains all 6 built-in patterns, Resolve() accepts 6 parameters</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Table-driven tests for Excludes and Force precedence</name>
  <files>internal/config/config_test.go</files>
  <read_first>
    - internal/config/config_test.go — read the full file to understand existing test structure (table-driven, t.Run, Resolve call signature pattern) before adding new tests
    - internal/config/config.go — read updated version after Task 1 to know the exact Resolve() signature and defaultExcludes values
  </read_first>
  <behavior>
    - TestResolveExcludes/defaults_when_no_input: Resolve with nil flagExcludes, empty file.Target.Exclude → cfg.Excludes contains all 6 default patterns, len == 6
    - TestResolveExcludes/file_extends_defaults: file.Target.Exclude=["*.tmp"] → cfg.Excludes contains all 6 defaults + "*.tmp", len == 7
    - TestResolveExcludes/flag_extends_file_and_defaults: flagExcludes=["logs/"], file.Target.Exclude=["*.tmp"] → all 6 + "*.tmp" + "logs/", len == 8
    - TestResolveExcludes/flag_deduplicates: flagExcludes=[".git/"] (already in defaults) → cfg.Excludes len == 6, ".git/" appears exactly once
    - TestResolveExcludes/file_deduplicates: file.Target.Exclude=[".git/"] → cfg.Excludes len == 6
    - TestResolveForce/default_false: no flag, no file → cfg.Force == false
    - TestResolveForce/file_sets_true: file.Target.Force=true, flagForce=false → cfg.Force == true
    - TestResolveForce/flag_overrides: file.Target.Force=false, flagForce=true → cfg.Force == true
    - TestResolveForce/both_true: file.Target.Force=true, flagForce=true → cfg.Force == true
    - All existing TestParseHost, TestResolveHostPrecedence, TestResolvePathPrecedence, TestResolveInvalidHostReturnsError, TestLoadFile tests pass unchanged (update Resolve() call sites in these tests to pass nil, false for the two new args)
  </behavior>
  <action>
    Update all existing Resolve() calls in config_test.go to the new 6-argument signature: `Resolve(flagHost, flagPath, nil, false, file, projectName)` — the nil and false are the new flagExcludes and flagForce parameters. Add TestResolveExcludes as a table-driven test with the 5 cases above. Add TestResolveForce as a table-driven test with the 4 cases above. Use the same t.Run pattern as the existing tests. Reference defaultExcludes indirectly by checking for specific strings (".git/", "*.log", etc.) or by length — do not import or access the unexported var directly; use Resolve() output.
  </action>
  <verify>
    <automated>cd /Users/mniedre/git/docker-deploy && go test ./internal/config/ -v -run "TestResolve|TestParseHost|TestLoadFile"</automated>
  </verify>
  <done>go test ./internal/config/ exits 0, TestResolveExcludes and TestResolveForce appear in output with all sub-cases PASS, all pre-existing tests still pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy.yaml → Config struct | User-controlled file content parsed and merged into runtime config |
| CLI flags → Config struct | User-supplied flag values merged into exclude list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `defaultExcludes` var | accept | Package-level var is unexported; callers receive it only through Resolve() output. Unit tests assert the 6 defaults are always present. |
| T-03-02 | Information Disclosure | `TargetConfig.Force bool` | accept | Force only skips a confirmation prompt, not a security gate. No secrets involved. |
</threat_model>

<verification>
go test ./internal/config/ -v exits 0
go vet ./internal/config/ exits 0
grep -c "Excludes \[\]string" internal/config/config.go returns 1
grep -c "Force.*bool" internal/config/config.go returns at least 2 (one in Config, one in TargetConfig)
grep -c 'defaultExcludes' internal/config/config.go returns at least 2 (declaration + usage)
</verification>

<success_criteria>
- Config.Excludes always contains the 6 built-in defaults when Resolve() is called with any input
- User excludes (file or flag) append to, never replace, the built-in list
- Force follows flag > file > false precedence
- All 24+ tests in config_test.go pass (including new Excludes and Force tests)
</success_criteria>

<output>
After completion, create `.planning/phases/03-file-copy/03-01-SUMMARY.md` using the summary template.
</output>
